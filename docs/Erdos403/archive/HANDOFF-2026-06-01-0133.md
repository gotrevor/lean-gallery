# Handoff: Erdős #403 — native_decide fully eliminated (0 axioms)

**Date**: 2026-06-01 01:33 UTC · **Branch**: `tier1-finiteness` · **HEAD**: `756c62f`

## 🎯 What we're doing
#403 was already SOLVED last session (largest power of 2 that's a sum of distinct
factorials is `2⁷=128`; `erdos_403_finite` + `erdos_403_sharp` in `Sharp.lean`).
This session's goal: **shrink, then eliminate, the `native_decide` trust** so the
proof is kernel-pure (mathlib-admissible). **Achieved.** Both headline theorems now
depend on exactly `[propext, Classical.choice, Quot.sound]` — nothing else.

## 🧠 Context to carry forward
- Trust journey this session: **7 → 3 → 2 → 0** native_decide axioms.
  - 7→3: four `m∈{8,9}` leaf cases → plain `decide` (commit `2eadf8e`).
  - 3→2: `two_pow_1620_odd` (`2^1620≡1 mod 467775`) → **CRT** proof (`eb0fe97`).
  - 2→0: the two 1620-case `base_offending`/`base_offending_sub` exhaustions →
    **kernel-pure `decide` over a residue fold** (`756c62f`). THE win.
- **Why the fold works** (the mental model): plain `decide` choked on the original
  because `Finset.Ico/Icc` reduce through `Multiset`/`Quot` (kernel poison) AND the
  numbers were `2^1629`-huge (blocked by `exponentiation.threshold=256`). Fix: a flat
  `List`/`Nat` `Bool` fold `checkAll` advancing a residue `adv r=(2r)%12!` (stays
  <479M), proved equal to the true residue by `res_pow` (`adv^[k] 1024 = 2^(10+k)%12!`).
  `decide` then reduces `checkAll 1620 1024 = true` in ~3s. Flushed as memory
  `lean-kernel-decide-large-finite-exhaustion` — reuse it for any big native_decide.
- **Approach 2 (Aristotle) is still RUNNING** (project `fcd8748d`, submitted ~01:22 UTC,
  the bounded base_offending lemma). It is **now moot for the goal** (we hit 0 without
  it) but worth harvesting: it might return a genuinely *structural* (non-exhaustive)
  proof — that was "Approach 3" and the real math glory. If it returns a native_decide
  proof, discard.
- Calibration: I estimated Approach 1 at 50%, raised to 80% after a 6.4s feasibility
  probe (kernel-decide the fold in isolation BEFORE writing bridge lemmas), landed 100%.

## ✅ State (all verified this session, not assumed)
- `lake build Erdos403.Sharp` → green, 8250 jobs (observed).
- `lake env lean Audit.lean` →
  `erdos_403_finite`/`erdos_403_sharp` depend on `[propext, Classical.choice, Quot.sound]`.
- Olean confirmed fresh vs source before the audit (dodged a stale-olean false-green).
- 3 commits this session on `tier1-finiteness`: `2eadf8e`, `eb0fe97`, `756c62f`.
  **All box-local, UNPUSHED** (no GitHub egress in the box).
- `.githooks/pre-commit` lakefile-detection bug fixed (`ls a b` → `[ -f a ]||[ -f b ]`)
  and **validated live** — it ran the build gate on `756c62f`.

## 🎬 Next actions
1. **Poll Aristotle**: `cd ~/src/erdos-403 && aristotle list | grep fcd8748d`. If
   `SUCCEEDED`/`IDLE`, `aristotle download fcd8748d --destination /tmp/ari.tar.gz` &&
   `tar -xzf`; read its proof. Keep only if it's a *different/structural* kernel-pure
   argument; otherwise note + discard.
2. **Update the docs** (Trevor offered/agreed this is wanted): `SOLVED.md` and master
   `HANDOFF.md` still say "3 standard axioms + native_decide" — now it's
   **fully kernel-pure, mathlib-admissible, 0 native_decide**. (Per repo convention I
   did NOT touch master `HANDOFF.md`/`SOLVED.md` mid-handoff.)
3. **Host push** (box can't): `git push origin tier1-finiteness` from the host, then PR.
4. Optional/aspirational: this is now genuinely **mathlib-PR-able** (passes lean4checker);
   also the formal-conjectures + erdosproblems trackers could be told #403 is formalized
   kernel-pure. Discuss scope with Trevor first.

## ⚠️ Gotchas
- **Box relay scrambles output** intermittently (stale/empty/interleaved). Verify from
  fresh sentinel greps + olean mtime; never chain a commit onto an unverified build.
  This session I committed a RED build once (`faf41a8`, amended away) because of this +
  the next bullet — caught it via audit. Stay paranoid.
- **`Edit` needs a real `Read`** — a `cat` in Bash does NOT satisfy it ("File has not
  been read yet"). Cost me two failed hook edits.
- `lake env lean <f>` type-checks but writes **no olean**; `#print axioms` needs the deps
  built via `lake build` first.
- Pre-commit hook only build-gates when a `.lean`/lakefile/toolchain/manifest file is
  *staged*; a docs-only or hook-only commit correctly skips it.

## 📁 Key files
- `src/Erdos403/Sharp.lean` — the proof. Kernel-pure `base_offending`/`_sub` machinery
  is ~lines 176–266 (`offendingB`/`adv`/`checkAll`/`checkAllSub`/`res_pow`/
  `checkAll_true`/`sub_res`/the two theorems). `two_pow_1620_odd` CRT proof ~110–143.
- `Audit.lean` — `#print axioms` regression check for both headline theorems (committed).
- `SOLVED.md`, `HANDOFF.md` — master docs, **need the "0 axioms" update** (action #2).
- `.githooks/pre-commit` — fixed lakefile detection.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor
or wait for instructions, and don't offer other projects from the KB — this doc IS the
chosen thread. Absorb the context, then start at Next action #1: poll Aristotle
(`aristotle list | grep fcd8748d`) and proceed.**
