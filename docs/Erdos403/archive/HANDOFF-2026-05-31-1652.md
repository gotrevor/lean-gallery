# Handoff: Erdős #403 — kernel isolated as `cascade_crux`; awaiting Lin/Frankl literature

**Date**: 2026-05-31 16:52 UTC · **Branch**: `tier1-finiteness` · **HEAD** `4b45e0f`

## 🎯 What we're doing
Proving Erdős #403 in Lean (`~/src/erdos-403`): only finitely many sums of distinct factorials are
powers of 2 (`erdos_403_finite`), sharp `m ≤ 7` (`erdos_403_sharp`). **Trevor's bar this session:
fully sorry-free is the only acceptable conclusion — complete it or abandon. He chose complete**,
reasoning (correctly) that this is a *known elementary theorem* (Lin & Frankl 1976, both proofs lost),
so a formalizable proof provably exists; the task is reconstruction, not discovery. The whole thing now
reduces to **one named `sorry`: `cascade_crux`** (`Basic.lean:482`).

## 🧠 Context to carry forward
**The kernel, exactly.** `cascade_crux` is the isolated 2-adic inequality
`v₂(oddpart(M!) + oddpart(factSum(S\{M}))) ≤ s₂ M + 2` (`M = max' S`), under the tied-bottom solution
hypotheses `min' S = 2 ∧ 3 ∈ S ∧ factSum S = 2^m`. Via `m_eq_top_val_add_lift` (descent ∘ lift:
`m = v₂(M!) + v₂(odd+odd)`) and Legendre `v₂(M!) = M − s₂ M`, this is **exactly equivalent** to the
carry ceiling `m ≤ M + 2`. Everything else — descent, lift, decomposition, even-`m` kill, the assembly
into finiteness + sharp — is proven and axiom-clean. `cascade_two` is sorry-free; it discharges its
`5∈S` branch *from* `cascade_crux`.

**Three things settled this session (don't re-attempt — each verified, two ways where noted):**
1. **No fixed modulus** closes the kernel (first FNS digit `≥2` index grows `~log m/log log m`).
2. **Size sieve insufficient** — the tight window `[M!, ∑_{a≤M}a!]` kills most `M` but a sparse
   infinite set survives (`63,64,90,161,255,256,…`), candidate `m≈290 ≫ M+2`. Only the valuation
   bound kills them.
3. **The unconditional `carry_gap` is FALSE** (PLAN.md had claimed `B=4` absolute — a small-`K`
   artifact). The family `{2ᵗ−2,2ᵗ−1,2ᵗ+1}` gives gap `2t−2 → ∞`. So the **power-of-two / odd-part-1
   hypothesis is essential**: `cascade_crux` is false without it; any proof must use the recursive
   structure of `factSum(S\{M}) = 2^m − M!`. (RECONSTRUCTION.md was right; PLAN.md is now corrected.)

**Why iterating the descent does NOT finish (abandoned route a).** The descent chain
`m > M−s₂M > M₁−s₂M₁ > …` strictly decreases *downward* from `m`, pinning each stripped sum's
valuation — but it never bounds `m` from *above*. The top lift `m − v₂(M!)` is exactly the CRUX and
stays free (the non-solution `{6,7,9}` has the identical chain shape with `m=13 > M+2=11`). Both the
top-down descent and the bottom-up cascade reduce to the same odd-part non-congruence. So this needs
**Lin's actual odd-part argument** (or a genuine new idea), not more valuation bookkeeping.

**Where this stands with Trevor.** He's opened a separate session to run `LITERATURE-REQUEST.md` (find
Lin/Frankl's proof on the open internet — the box is firewalled). That is the real unblock. I
deliberately **stopped the blind grind** at the math wall rather than risk fabricating a flawed
odd-part argument. When the literature returns Lin's key 2-adic step, translating it into a proof of
`cascade_crux` is the mechanical finish → fully sorry-free.

## ✅ State (verified this session)
- **Build green**: `lake build Erdos403.Sharp` → 8250 jobs, "Build completed successfully". Tree clean
  at `4b45e0f`.
- **Single source `sorry`**: `Basic.lean:482`, inside `cascade_crux`.
- New axiom-clean lemmas (each `#print axioms` = propext/Classical.choice/Quot.sound only):
  `v2_factSum_erase_max` (descent), `v2_add_of_v2_eq` (lift), `m_eq_top_val_add_lift` (decomposition),
  `six_dvd_factorial`, `four_pow_mod_six`. `erdos_403_finite`/`_sharp` axioms = standard three +
  `sorryAx` (via `cascade_crux` only).
- Box: local commits only on `tier1-finiteness` — **host must push** (no egress here).

## 🎬 Next actions
1. **If the literature session has returned** (check `~/Downloads/erdos403-refs/` or ask Trevor): read
   the transcribed Lin/Frankl odd-part step and prove `cascade_crux` (`Basic.lean:482`). Goal there is
   the inequality above; the odd-`m` foothold is already proven inside the lemma. `rg` existing lemma
   names first (`read-repo-before-rederiving`) — the `v2_factorial_*` engine + the descent/lift are
   ready to use.
2. **If not yet returned**: do NOT blind-grind the proof. Optional positive-EV probe only — the
   product-of-odds recursion `oddpart(n!) = (∏_{odd j≤n} j)·oddpart(⌊n/2⌋!)` for `oddpart(M!) mod 2^k`
   (any CRUX proof will use it). Don't manufacture a proof.
3. If banking instead: host `git push -u origin tier1-finiteness`; consider refreshing master
   `HANDOFF.md` (don't overwrite mid-thread).

## ⚠️ Gotchas
- `lake build` "0 jobs" after an Edit = false green (mtime); confirm a real job count. Box OOM:
  `pkill -9 lean lake` and re-run.
- Pre-commit hook prints "Could not locate a lakefile — skipping" (cwd quirk) — harmless; verify
  builds manually.
- `cascade_two`'s `#print axioms` shows `sorryAx` — that's *transitive* via the `cascade_crux` call;
  `cascade_two`'s own body is sorry-free. Don't mistake this for a second sorry.
- For omega+`set` over division terms (`a/2^k`), see the staging-buffer note flushed this session.

## 📁 Key files
- `src/Erdos403/Basic.lean` — everything: valuation engine, descent/lift/decomposition, `cascade_crux`
  (lone `sorry` @ L482), `cascade_two`, assembly. `FactBase.lean` — FNS digits (alt framing).
- `LITERATURE-REQUEST.md` — the brief for the host/internet session (the real unblock).
- `PLAN.md` — staged plan + crux localization + `carry_gap` refutation. `RECONSTRUCTION.md` — strategy.
- Prior snapshot: `HANDOFF-2026-05-31-0530.md` (pre-this-session, kernel was buried in `cascade_two`).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other KB projects — this doc IS the chosen thread. First check whether
the literature session has returned Lin/Frankl's proof (Next action #1); if yes, prove `cascade_crux`
from it; if not, do NOT blind-grind — probe only as in action #2.**
