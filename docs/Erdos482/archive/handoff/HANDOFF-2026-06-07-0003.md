# Handoff: Erdős #482 — the ENTIRE St05 paper is formalized (axiom-clean)

**Date**: 2026-06-07 00:03 UTC · **Branch**: main · **HEAD**: `36557fd` (lap end)

## 🎯 What we're doing
Formalizing Stoll's digit-extraction work for Erdős–Graham #482 in Lean 4 / mathlib v4.29.1. This lap
**completed the whole St05 paper** — the real #482 resolution: for ANY `w>0` and ANY base `g≥2`, an
explicit floor-recurrence reads off `w`'s base-`g` digits. (Previously only the `0902.4168` α√2
headline + Thm 3.2/Cor 3.3 + pair 5 were done; St05 was a fresh scaffolded track.)

## 🧠 Context to carry forward
- The prior lap had handed the **Thm 1.3 closed-form joint induction** to Aristotle (job `e0240fef`),
  which **stalled at 9%**. I cracked it **locally** instead, by decomposing into two single-step floor
  identities (`step_eo` even→odd, the crux; `step_oe` odd→even, exact) + a clean `Nat.rec` joint
  induction. **Lesson: decompose the all-in-one obligation into per-step floor lemmas — far more
  tractable than handing the whole induction to one solver.** Aristotle helper jobs were submitted but
  I beat them locally every time; treat them as insurance, not the critical path.
- **The math, all hand-verified before formalizing** (St05's printed pair-5 claim was once *wrong*, so
  I never trust a printed closed form): each step reduces to `(denom)·(a·P − target) = explicit poly`,
  bounded into `[0, denom)` by the floor/parameter facts. Key reductions are in each lemma's docstring.
  Numerics in `tools/sandbox/st05_thm13_verify.py` (Thm 1.3) and `st05_thm12_verify.py` (Thm 1.2 both
  cases, all `j`, ε endpoints) — both PASS. Always run these before formalizing a new closed form.
- **Thm 1.2 has TWO cases** sharing the binary recurrence `gv` (even step `(a,½)`, odd step `(b,ε)`):
  Case II (ε=½, point) and Case I (ε∈[1/3,2/3), an INTERVAL — the oe step is proved uniformly over the
  whole interval). The odd-index closed form `2^k+⌊t·2^k/2⌋` is **shared** across Thm 1.2 I/II and
  Thm 1.3 at g=2 — so digit extraction always reuses `thm13_digit_realDigits`.
- Use the **`/g` convention** (`t·g^k/g`, not `t·g^{k-1}`) everywhere to dodge the `k=0` negative-
  exponent / ℕ-truncation bug.
- See flushed gotchas (staging buffer): nlinarith `2^(k+1)`-vs-`2^k` atom mismatch; nlinarith heartbeat
  blowup on symbolic `b`/def-casts (bound the cleared `Den·X` form + transfer sign, don't divide);
  `Real.digits` needs `[NeZero g]` binder; `ε=0` fails Thm 1.3's range at g=2 (use `ε=−1/g`).

## ✅ State (verified green this lap: `lake build` = 8265 jobs; every new decl `#print axioms` clean)
12 commits (`d846045`..`36557fd`). Entire St05 in `src/Erdos482/General/`, all
`[propext, Classical.choice, Quot.sound]`-only, zero `sorry`, zero custom axioms (repo-wide):
- **Thm 1.3** (g-ary headline) — `Thm13Closed.lean`: `thm13_closed` (closed forms by joint induction) +
  `thm13_digits` (digit extraction) + `digit_recon` (output = literal `Real.digits t g (n−2)`).
- **Thm 1.2 Case II** — `Thm12.lean` (`gv` def + `_eo`/`_oe`/`_closed`/`_digits`).
- **Thm 1.2 Case I** (ε-interval) — `Thm12CaseI.lean` (same shape; `_oe` uses `maxHeartbeats 800000`).
- **Thm 1.1** (Rabinowitz–Gilbert) — `Thm11.lean` (= Case I `j=1,ε=½`).
- **Cor 1.1** (both √2 binary families) — `Cor11.lean`; **Cor 1.2** (ternary √2) — `Cor12.lean`;
  both have `_literal` forms (= `Real.digits √2 b (n−2)`, the actual mathlib digits of √2).
- **Top-level** — `Erdos482General.lean`: `erdos482_resolution` (any w>0) + `erdos482_resolution_literal`
  (w∈[1,g), literal w-digits).

## 🎬 Next actions
1. **St06 is the next frontier but is BLOCKED on an online fetch** — see `ON-LINE-REQUEST.md` (filed this
   lap; Acta Arith. 125 (2006), "sharper" companion). A networked host must drop `ON-LINE-FINDINGS-*.md`.
   Until then, do NOT spin wheels on St06. **First check for a findings doc before re-requesting.**
2. **Aristotle job `d33fa0c4` (digit_recon)** — submitted late, now REDUNDANT (proved locally). Poll
   `aristotle list`; if done, ignore/harvest for cross-check; submit a fresh job only if you start new
   hard content.
3. **Optional polish** (low priority, do only if no St06): more literal-digit showcases for other
   constants (π base 10, √3 base 2) via `erdos482_resolution_literal`; or connect the general
   `erdos482_resolution` (general w) to literal `Real.digits w g ·` through the mantissa offset
   `m=⌊log_g w⌋` (the one index-shift I deliberately left — it's bookkeeping, not insight).

## ⚠️ Gotchas
- Repo is **fully axiom-clean with ZERO open obligations** — this is a genuine completion, not a stuck
  point. Don't manufacture busywork; the honest next step is St06 (blocked) or polish.
- Pre-commit hook runs `lake build`; never claim green you didn't see. NEVER push (host pushes).
- `Int.floor_intCast_add` (not `Int.floor_add_int`); `div_lt_div_iff_of_pos_right` (not `div_lt_div_iff`).

## 📁 Key files
- `src/Erdos482/General/*.lean` — the St05 track (see State above).
- `tools/sandbox/st05_thm1{2,3}_verify.py` — numeric oracles; run before formalizing any new closed form.
- `notes/ST05-GENERAL-PLAN.md` (top banner = COMPLETE) · `STATUS.md` (🏆 section) · `ON-LINE-REQUEST.md`.

---
**→ Next session: this doc IS the chosen thread — don't summarize it back to Trevor, don't offer other
KB projects. St05 is DONE and committed. Start at Next action #1: check for an `ON-LINE-FINDINGS-*.md`
on St06; if none, the productive move is the optional polish in #3 (a π/√3 literal-digit showcase is a
quick, satisfying win). Read the flushed gotchas before touching nlinarith on a new floor identity.**
