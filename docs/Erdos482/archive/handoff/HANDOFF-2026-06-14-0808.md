# Handoff: cubic path-#2 — equidistribution backbone DONE; defect-link 3 pieces left

**Date**: 2026-06-14 08:08 UTC · **Branch**: `st06` · **HEAD**: `3165cbc` (+ this doc)

## 🎯 What we're doing
Formalizing Stoll [St06] in Lean 4 — the "for-fun" frontier is the **unconditional a.e.-`W` cubic
self-referential impossibility** (attack-path #2). All #482/St05/St06 headline theorems are done &
axiom-clean. This thread proves: *for almost every real `W`, no fixed 3-periodic offset schedule
`(c₀,c₁,c₂)` makes the cubic three-step floor map `⌊α(⌊α(⌊α(u+c₀)⌋+c₁)⌋+c₂)⌋` (`α=2^{1/3}`) read `W`'s
base-2 digits.* This lap built the **entire analytic backbone + the algebraic half of the geometry
finish**, all axiom-clean (`[propext, Classical.choice, Quot.sound]`), full `lake build` 🟢 8286.

## 🧠 Context to carry forward (the mental model — read this)
- **The reduction collapsed beautifully this lap.** The old framing (combined defect `α²f₁+αf₂+f₃` in a
  width-1 window) is superseded: the **third floor error is forced**, `f₃ = {C−g}` (`cubic_f3_eq`), so
  the whole defect collapses to `cubicDefect = C − ⌊C−g⌋` and the extracted **digit `= ⌊C−g⌋`**
  (`cubic_digit_eq_floor`), where `g := α²f₁+αf₂` (the *two*-floor partial defect) and `C := 2c₀+α²c₁+αc₂`.
  So **a base-2 digit ⟺ `g ∈ (C−2, C]`** (`cubic_partial_defect_mem_window`), a width-2 window. Since `g`
  ranges over `[0, α²+α)` with `α²+α ≈ 2.85 > 2`, no window holds the whole range ⟹ contradiction once the
  orbit explores it. This is *cleaner and sharper* than the width-1 story — internalize it.
- **Why density (not just the old ergodic density) suffices now.** Earlier handoffs feared needing full
  equidistribution on the measure-zero curve. With the `g`-reduction, the route is: (i) `g(⌊W2ⁿ⌋)` is an
  explicit function `cubicGpd` of the orbit's canonical fractional coords `(r₁,r₂,r₃)=({W2ⁿ},{αW2ⁿ},{α²W2ⁿ})`
  (`cubicPartialDefect_eq_Gpd`), continuous off the floor-jump surfaces (`continuousAt_cubicGpd`); (ii) the
  orbit `(2ⁿW,2ⁿαW,2ⁿα²W) mod 1` is **dense** in `T³` for a.e. `W` (`ae_W_cubic_torus_orbit_dense`, built
  from the full equidistribution backbone); (iii) at a continuity point where `cubicGpd` leaves `(C−2,C]`,
  the dense orbit realizes an out-of-window defect (`exists_lt_of_dense_continuousAt`) ⟹ a digit `∉{0,1}`.
- **Weyl reduction details (already done, for reference):** torus character `mFourier m (orbitₙ) =
  e(2πi·2ⁿ·ξW)`, `ξ=m₀+m₁α+m₂α²`; `cubicXi_ne_zero` (from `cubic_lin_indep_int`) gives `ξ≠0`, so it's the
  freq-1 doubling Weyl sum at `s=ξW`; `ae_doubling_weyl_tendsto_real 1` scaled by `ae_comp_mul_left` gives
  per-`m` a.e.-`W` vanishing; `ae_all_iff` + `weyl_criterion_torus` ⟹ equidistribution.
- **The periodicity bridge** (`ae_of_ae_restrict_Icc01_of_periodic`) lifts a.e.-[0,1] facts to a.e.-ℝ for
  unit-periodic predicates (ℤ-invariant null bad set = countable union of `measure_preimage_add_right`
  translates). It's what let `ae_comp_mul_left` (needs full `volume`) consume the `[0,1]`-restricted DEL.

## ✅ State (all axiom-clean, full `lake build` 🟢 8286 observed)
New this lap (9 commits): `DoublingEquidist` (periodicity bridge, `…_real` forms), `EquidistDense`
(`isEquidistributed_dense` [Aristotle `3e68d32f`], `…Torus_dense`, `exists_lt/gt_of_dense_continuousAt`,
`dense_continuousOn_image_subset` [Aristotle `291bb0fb`]), **`CubicTorusEquidist`**
(`ae_W_cubic_torus_orbit_equidistributed` + `…_dense`), `CubicDefect` (the `g`-collapse: `cubic_f3_eq`,
`cubicDefect_eq_C_sub_floor`, `cubic_digit_eq_floor`, `cubic_partial_defect_mem_window`),
`CubicDefectLink` (`cubic_f1_orbit`, `cubic_f2_orbit`, `cubicGpd`, `cubicPartialDefect_eq_Gpd`,
`continuousAt_cubicGpd`). All wired into `Erdos482General.lean`.

## 🎬 Next actions
1. **`aristotle list`** → harvest **`cubicGpd_exceeds_window` (`7b1ff2ad`, gpdwin)** when IDLE — the
   geometric crux: ∀`C`, ∃`(r₁,r₂,r₃)∈(0,1)³` with the two `fract`-args non-integer and `cubicGpd ∉ (C−2,C]`.
   Verify in-kernel + `#print axioms`; port into `CubicDefectLink`. (If it failed, prove locally: realize any
   `(fA,fB)∈(0,1)²` via `r₁=½`, solve `r₂,r₃`; case-split `C ≶ α²+α>2`; high/low fracts.)
2. **Piece 2 — `F`-on-`T³` continuity glue** (API verified this lap, see PENDING_WORK ★★★): define
   `ρ x := (AddCircle.equivIco 1 0 x : ℝ)`; `ρ(↑t)=Int.fract t` via `AddCircle.coe_equivIco_mk_apply`+`simp`;
   `ContinuousAt ρ x` for `x≠0` via `AddCircle.continuousAt_equivIco hx` ∘ `continuous_subtype_val`
   (explicit `(g:=)(f:=)` in `ContinuousAt.comp`!). Then `F a := cubicGpd α c0 c1 (ρ(a 0))(ρ(a 1))(ρ(a 2))`,
   `F(cubicTorusOrbit W n) = cubicGpd(reps)`, `ContinuousAt F p*` at `p*` with coords ≠0 & fract-args
   non-integer.
3. **Piece 3 — final assembly**: pick `p*` from (1), apply `exists_lt_of_dense_continuousAt` with a.e.-`W`
   density, get out-of-window `n`, contradict `cubic_partial_defect_mem_window`. State the headline as
   "for a.e. `W`, ∃n, `cubicV3(⌊W2ⁿ⌋)−2⌊W2ⁿ⌋ ∉ {0,1}`". (`cubic_block_orbit_base_two_bounds` ties the floor
   orbit to a digit-reading orbit if you want the recurrence-form statement.)

## ⚠️ Gotchas
- `continuousAt_fract` (root namespace, NOT `Int.`), hyp `x ≠ ⌊x⌋`, from `Mathlib.Topology.Algebra.Order.Floor`.
- `ContinuousAt.comp` through `Int.fract`/`Subtype.val`: **must** pass explicit `(g:=)(f:=)` or it picks the
  wrong `HAdd.hAdd` decomposition (flushed to KB staging).
- `simp only [Int.fract]` can fully close an arithmetic goal — a trailing `ring`/`ring_nf` then errors
  "no goals"; use `ring_nf` (no-op-safe) or drop it.
- DO NOT push (Trevor merges). Pre-commit runs full `lake build`; commit every green build.

## 📁 Key files
- `src/Erdos482/General/CubicDefect.lean` — the `g`-collapse + window (`cubic_partial_defect_mem_window`).
- `src/Erdos482/General/CubicDefectLink.lean` — `cubicGpd`, orbit identities, `continuousAt_cubicGpd`.
- `src/Erdos482/General/CubicTorusEquidist.lean` — `ae_W_cubic_torus_orbit_{equidistributed,dense}`.
- `src/Erdos482/General/EquidistDense.lean` — density⇒dense + the threshold/confinement tools.
- `PENDING_WORK.md ★★★` (top) — the authoritative 3-piece finish plan with verified API snippets.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor, don't wait for
instructions, and don't offer other KB projects — this doc IS the chosen thread. Absorb the `g`-collapse
mental model above, then start at Next action #1: `aristotle list` → harvest `cubicGpd_exceeds_window`
(`7b1ff2ad`), then drive pieces 2+3 to close the unconditional a.e.-`W` cubic.**
