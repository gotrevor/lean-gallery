# Handoff: Erdős #482 — pair 5 RESOLVED + St05 general track started

**Date**: 2026-06-06 ~23:00 UTC · **Branch**: main · **HEAD**: `b127a52`

## 🎯 What we're doing
Formalize Stoll's digit-extraction work for Erdős #482 (Lean 4 / mathlib v4.29.1). The headline
(Graham–Pollak / digits of √2) and bonus (Thm 3.2 for 7/8 pairs + Cor 3.3) were already complete &
axiom-clean. This lap (1) **resolved pair 5** and (2) **opened the St05 general track** (the real
#482 resolution: any `w>0`, any base `g≥2`).

## ✅ State: `lake build` green = 8258 jobs · zero `sorry` · zero custom axioms (repo-wide, verified)
Every theorem `#print axioms` = `[propext, Classical.choice, Quot.sound]`. 16 commits this lap
(`6fde44e`..`b127a52`).

### Pair 5 — RESOLVED (the full interval is NOT a theorem; honest content formalized)
The online findings (`archive/findings/ON-LINE-FINDINGS-2026-06-06-pair5.md`, harvested) established:
Stoll's full-interval pair-5 claim is *false* (fails at n=280 at the stated endpoint); only ε=½ works
for all n (= the headline). Printed §4 closed form has a typo. Formalized in `src/Erdos482/Stoll.lean`:
- `stoll_pair5_closed_form` — typo-corrected §4 explicit formula (via `gp_pair`+`vv_half_eq_u`).
- `pair5_estep_band` — **exact band characterization**: the ε-step lands iff
  `B_j(ε):={√2·2^j}−√2·{√2·2^{j−1}}+√2ε ∈ [0,1)`.
- `stoll_pair5_conditional` — honest conditional full-interval theorem (base step + band ∀j ⟹ digits).
- `pair5_band_at_half` (B_j(½)=crux) + `stoll_pair5_half_via_band` (GP re-derived via band route).
- `pair5_band_branch` (two-branch identity) + `pair5_band_fails_below_half`/`pair5_band_fails_above_half`
  (**precise obstruction**, both sides: no ε≠½ is uniformly admissible).
- Diophantine infra: `fract_two_mul`, `fract_two_mul_branch`, `fract_sqrt2_pow_ne_half`,
  `sqrt2_pow_far_from_halfint`, (existing) `sqrt2_badly_approximable`.
**No open obligation remains on pair 5.** The admissible ε-set is exactly the band condition (a
Diophantine condition, plausibly `{½}` if √2 is normal — open). See PENDING_WORK §1.

### St05 general track — STARTED (`src/Erdos482/General/`, wired into root)
Stoll [St05] (J. Integer Seq. 8 (2005)) resolves #482 for any `w>0`, any base `g≥2`. Plan:
`notes/ST05-GENERAL-PLAN.md`; verbatim targets: `papers/SOURCES.md`. **Thm 1.3 numerically verified**
(`tools/sandbox/st05_thm13_verify.py`: g∈{2,3,10}, w∈{√2,√3,π}, ε-endpoints, many n — all OK).
- `General/Digits.lean` — `digitStep g x = ⌊gx⌋−g⌊x⌋` + range bound `digitStep_mem` (0≤·<g); `gdigit`
  (Prop 2) + `gdigit_mem`; **`realDigits_eq_digitStep`** = general-base Prop 2 bridge to mathlib
  `Real.digits` (generalizes the repo's base-2 `digits_eq_floor_sub`).
- `General/Thm13.lean` — `thm13_digit_of_oddClosed`: given the odd closed form
  `u(2k+1)=g^k+⌊t·g^k/g⌋`, the difference `u(2n+1)−g·u(2n−1)=digitStep g (t·g^{n−1}/g) ∈ [0,g)`;
  `thm13_digit_realDigits`: same, expressed as a literal mathlib digit `Real.digits (t·g^{n−1}/g) g 0`.
- `General/Mantissa.lean` — `mantissa_mem` (1≤t=w/g^m<g) + `mantissa_reconstruct` (t·g^m=w).

## 🔴 IN-FLIGHT ARISTOTLE JOB — HARVEST FIRST NEXT LAP
**`thm13closed` = project `e0240fef-d8a6-4c98-91c2-74f6401295ed`** (submitted this lap, still RUNNING
at ~17min). It proves St05 **Thm 1.3's closed-form joint induction** (recurrence ⟹ both closed forms):
the LAST piece needed for an UNCONDITIONAL Thm 1.3. Problem file: `tools/aristotle/thm13closed/Problem.lean`.
**Next lap step 1:** `aristotle show e0240fef-…` ; if COMPLETE, `aristotle download`, extract, VERIFY
in our kernel + `#print axioms`, then:
  1. Port it onto a repo recurrence `gu` def (define `gu` in `General/Thm13.lean` matching the problem's
     `hrec`/`hu0`; the problem statement is faithful to the verified numerics — gu-indexed,
     `gu n = u_{n+1}`, step uses (a,ε) when index even, (b,1/(g-1)) when odd).
  2. Chain `thm13_closed → thm13_digit_of_oddClosed` (note: convert the `hB:(g-1)·gu(2k+1)=g^k−1`
     even-form and the `hA` odd-form; `thm13_digit_of_oddClosed` only needs the odd form `hA`, which is
     literally the `gu(2k)` closed form — mind the gu vs u indexing: `gu(2k)=u_{2k+1}`).
  3. Result: unconditional **St05 Theorem 1.3** (g-ary digits of any w). Then submit the next Aristotle
     job and continue the plan.
NOTE: Aristotle's progress log mentions "even_step: disproved" — that was its OWN scratch-lemma
variable-ordering bug (k auto-bound), NOT a flaw in the problem statement (which matches the verified
numerics). Don't be alarmed; it kept going and was deep in the correct floor-identity proof.

All other Aristotle jobs this lap are COMPLETE + harvested: `de36c4a0` (fract_two_mul), `87fe630d`
(fract_sqrt2_pow_ne_half), `eb453bc4` (sqrt2_pow_far_from_halfint), `d8f5da47` (fract_two_mul_branch),
`17d71595` (pair5_band_branch); `e73fd464` (mantissa) cancelled (proved locally).

## 🎬 Next actions (in order)
1. **Harvest `thm13closed`** (above) → unconditional St05 Thm 1.3. Keep one Aristotle job in flight.
2. **St05 plan milestones** (`notes/ST05-GENERAL-PLAN.md`): after Thm 1.3, do Thm 1.2 Cases I/II
   (binary two families) — NOTE their digit extraction reuses `thm13_digit_of_oddClosed` at g=2 (the
   odd closed form `u(2k+1)=2^k+⌊t·2^{k−1}⌋` is identical), so only the closed-form inductions differ
   (hand each to Aristotle). Then Cor 1.1 (√2 binary family) / Cor 1.2 (ternary √2) as cross-checks.
3. **Prop 2 proper / reconstruction**: optionally connect `digitStep` to the n-th base-g digit of the
   *fractional part* of `w` (the t≥1 vs `Real.digits` domain [0,1) shift) — polish, not blocking.

## ⚠️ Gotchas (this lap)
- `Int.floor_eq_iff` (ℤ version) takes NO hypothesis argument (unlike `Nat.floor_eq_iff`).
- `omega` cannot reason about `g*M` with both `g,M` variables (nonlinear) — for `N % g = N − g*M` use
  `Int.add_mul_emod_self_left` + `Int.emod_eq_of_lt` (see `realDigits_eq_digitStep`).
- After `obtain ⟨i, rfl⟩ : ∃ i, j = i+1`, normalize `i+1-1 → i` with `simp only [Nat.add_sub_cancel]`
  so `linarith`/`rw` see one atom (`2^(i+1-1)` vs `2^i`).
- `Real.digits x b` is GENERAL-base in mathlib v4.29.1 (`[NeZero b]`); add `[NeZero g]` as an instance
  binder when a *statement* mentions `Real.digits … g …`.
- Pre-commit gate runs `lake build`; never claim green you didn't see.

## 📁 Key files
- `src/Erdos482/Stoll.lean` — all pair-5 work (band characterization + obstruction).
- `src/Erdos482/General/{Digits,Thm13,Mantissa}.lean` — the St05 track.
- `PENDING_WORK.md` §1 (pair 5 RESOLVED), §1b (St05 in progress), §0 + `notes/ST05-GENERAL-PLAN.md`.
- `tools/aristotle/thm13closed/Problem.lean` — the in-flight job's problem.
- `tools/sandbox/st05_thm13_verify.py` — Thm 1.3 numerics.

---
**→ Next session: start at Next actions #1 — `aristotle show e0240fef-…`, harvest the closed-form
induction, and land unconditional St05 Theorem 1.3. The repo is green & axiom-clean; don't re-open
pair 5 (resolved). This is the chosen thread — continue the St05 general track.**
