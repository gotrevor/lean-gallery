# Handoff: general degree-`d` impossibility — reduced (in Lean) to ONE geometry-crux hypothesis

**Date**: 2026-06-14 12:05 UTC · **Branch**: `st06` · **HEAD**: `697984b` (+ this doc)

## 🎯 What we're doing
Stoll [St06] in Lean 4. #482/St05/St06 headlines + the cubic & quartic a.e.-`W` self-referential
impossibilities are all DONE & axiom-clean. **This lap built the entire general degree-`d`
(`α = 2^{1/d}`) development** and reduced the uniform headline — *explicitly in Lean* — to a single
geometry-crux hypothesis. Repo is sorry-free + custom-axiom-free; full build 🟢 **8297 jobs** (observed);
every new theorem `#print axioms` = `[propext, Classical.choice, Quot.sound]` (verified this lap).

## 🧠 Context to carry forward
- **The whole obstruction in one breath.** The `d`-step floor map's extracted digit is `⌊C−g⌋`
  (`C = ∑α^{d-k}cₖ`, partial defect `g = ∑_{k<d-1} α^{d-1-k} fₖ`, last floor forced). A base-2 digit ⇒
  `g ∈ (C-2,C]` (width 2). But `g` ranges over `[0, S_d)`, `S_d > 2` for `d ≥ 3` (`⟺ 2^{1/d} < 4/3`),
  so a *dense* orbit must leave the window. `d = 2` gives `√2 < 2` — obstruction begins exactly at `d=3`.
- **Why this lap moved fast.** The analytic engine (`MultidimWeyl`/`EquidistDense`/`DELEngine`) is
  already degree-agnostic, so `Tᵈ` density was a near-mechanical port of `CubicTorusEquidist`. The genuinely
  new pieces were: (a) `rpow_lin_indep_int` via **Eisenstein** (the per-degree obstacle that the cubic
  elimination / quartic 2-adic descent don't generalize past — see memory
  `[[lean-eisenstein-to-Q-irreducible-via-isprimitive-gauss]]`); (b) `orbitF` as a `Nat.strongRecOn`
  recursion (`[[lean-strongrecon-recursion-under-finset-sum]]`).
- **The ONE remaining piece — and why it's the hard part.** `ae_dStep_fails_of_exceeding` takes a torus
  point `P` with **nonzero coords** + non-jump args + `dGpdTorus P ∉ window` as hypothesis. The crux is the
  *nonzero-coord* realization: `realizeR` sets `r 0 = 0` (so `P 0 = 0`, breaks `torusRep` continuity). Fix
  is a recursive representative construction `ρ 0 = σ`, `ρ(k+1) = fract(τ − restₖ(σ))`, choosing `σ ∈ (0,1)`
  outside a **countable** bad set (each `{σ | τ−restₖ(σ) ∈ ℤ}` is an affine-preimage of `ℤ`, countable;
  `Ioo 0 1` uncountable). This is genuinely ~80–100 lines (countability + induction); I deliberately did
  NOT rush it near budget end and left it cleanly documented rather than risk a messy state.

## ✅ State (all axiom-clean, build 🟢 8297; 28 commits this lap)
- **Algebra**: `RpowLinIndep.rpow_lin_indep_int` (Eisenstein, all `d`); `RpowWindow` (`rrt_window_gt_two`,
  `window_not_cover`, `exists_scale_outside_window`, `sum_pos_coeff_realize`,
  `exists_partial_defect_outside_window`, `rrt_pow_self`/`rrt_pos`); `GeneralDefect`
  (`affine_rec_closed`, `dStep_defect_identity`, g-collapse, `dStep_partial_mem_window`, range bounds).
- **Faithfulness** (`GeneralDefectCheck`): `cubicV3_sub_eq_via_general`, `quarticV4_sub_eq_via_general`
  (general engine reproduces the verified cubic & quartic).
- **Analytic bridge**: `GeneralOrbit` (`orbitF`/`orbitF_eq`, `dStepF_eq_orbitF`, `dGpd` +
  `dStepPartial_eq_dGpd`, `continuousAt_dGpd`, `orbitF_realizeR`, `orbitF_congr`/`dGpd_congr`);
  `GeneralTorusEquidist.ae_W_dTorus_orbit_dense`; `GeneralTorusFinish` (`dGpdTorus_orbit`,
  `continuousAt_dGpdTorus`, **`ae_dStep_fails_of_exceeding`** = the reduction).

## 🎬 Next actions
1. **Discharge the geometry crux** to make the headline unconditional. Prove
   `exists_exceeding_torus_point`: for `d ≥ 3`, schedule `c`, ∃ `P : Fin d → AddCircle 1` with
   `(∀ i, P i ≠ 0)`, the `orbitArg`s ∉ ℤ, and `dGpdTorus d (rrt d) c P ∉ (dStepC … − 2, dStepC …]`.
   Build it via the recursive `ρ`/countability construction above (ingredients ready:
   `exists_scale_outside_window`, `orbitF_realizeR`, `rrt_window_gt_two`). Then feed to
   `ae_dStep_fails_of_exceeding` → `ae_no_dStep_schedule_reads_base_two (d) (hd : 3 ≤ d)` (quantify `c`
   inside the `∀ᵐ`, mirroring `CubicFinish.ae_no_cubic_schedule_reads_base_two`).
2. Then the capstones (digit-representable / reads-base-two forms), mirroring `CubicFinish`/`QuarticFinish`.
3. **Aristotle is IDLE** — the nonzero-coord realization is a clean self-contained candidate to submit
   (inline `dGpd`/`orbitF`/`orbitArg` defs + the ready lemmas as axioms; goal = the `∃ P …`).

## ⚠️ Gotchas (full list in HANDOFF-2026-06-14-1145.md)
- `import Mathlib.Algebra.BigOperators.Basic` → use `…BigOperators.Group.Finset.Basic`.
- Eisenstein→ℚ: use the **`IsPrimitive`** Gauss variant, NOT `Monic.…` (needs absent `IsIntegrallyClosed ℤ`).
- New file in `Erdos482.General` namespace: rename lemmas that clash with cubic/quartic (e.g.
  `mFourier_orbit_eq` → `dmFourier_orbit_eq`) or hit "environment already contains …" on the aggregator
  import. **Wire every new file into `Erdos482General.lean`** or the pre-commit `lake build` gate won't check it.
- `ContinuousAt.comp` HO-unification: pass explicit `(g := …) (f := …)`.

## 📁 Key files
- `src/Erdos482/General/GeneralTorusFinish.lean` — `ae_dStep_fails_of_exceeding` (the reduction) + plumbing.
- `src/Erdos482/General/GeneralOrbit.lean` — `dGpd`, `orbitF`, `realizeR`, continuity.
- `src/Erdos482/General/{RpowLinIndep,RpowWindow,GeneralDefect}.lean` — the algebraic obstruction.
- `PENDING_WORK.md` (top ★★★★★) + `HANDOFF-2026-06-14-1145.md` — the exact construction for the crux.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor, don't wait for
instructions, and don't offer other KB projects — this doc IS the chosen thread. The general-`d`
impossibility is reduced in Lean to `ae_dStep_fails_of_exceeding`; pick up at Next action #1 — prove
`exists_exceeding_torus_point` (nonzero-coord realization via the countability construction) and feed it in.**
