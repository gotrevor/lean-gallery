# Handoff: base-`g` general-degree impossibility — COMPLETE for every base `g ≥ 2`

**Date**: 2026-06-14 12:28 UTC · **Branch**: `st06` · **HEAD**: `5e8bc90`

## 🎯 What we're doing
Stoll [St06] / Erdős #482 in Lean 4. The base-2 general degree-`d` self-referential impossibility was
already DONE & axiom-clean (`GeneralTorusFinish`). **This lap built the entire base-`g` generalization**
(`α = g^{1/d}`, base-`g` digits `{0,…,g-1}`, window width `g`) and took it to *full parity* with the
base-2 story, for **every** base `g ≥ 2`. Repo is sorry-free + custom-axiom-free; full build 🟢 **8304
jobs** (observed this lap), every new theorem `#print axioms = [propext, Classical.choice, Quot.sound]`
(verified).

## 🧠 Context to carry forward
- **The whole base-`g` port was a near-mechanical mirror of base 2**, because two structural facts hold:
  (1) the *analytic engine* (Weyl L², DEL, equidistribution, torus density) is base-agnostic except for
  `n ↦ gⁿ` injectivity (`g_pow_inj` replaces `two_pow_inj`); (2) the *orbit bridge* used `W·2ⁿ` only as a
  product unit, so it generalizes to any real `M` (`dStepF_orbit_real` etc., `M = W·gⁿ`). See memory
  `[[lean-generalize-multiplier-to-abstract-real]]`. The plumbing (`coordsOf`, `dGpdTorus`,
  `continuousAt_dGpdTorus`) and the geometry-crux construction (`realizeR0`, `orbitF_realizeR0`, the
  countable-bad-set seed choice) are abstract in `α`/`c` and were reused **verbatim**.
- **The one genuinely-new obstacle was lin-indep of `{1, g^{1/d}, …}` for base `g`.** Eisenstein
  (`rpow_lin_indep_int_base`, prime `p ∥ g`) only covers squarefree-ish `g` — it **misses perfect powers**
  (`4 = 2²`). The fix that covers ALL `g`: take `d` **prime** (the window bound `g^{1/d} < 2g/(g+1)` only
  needs `d` large, and primes are unbounded) and use the Kummer criterion
  `X_pow_sub_C_irreducible_of_prime` — its input `∀ b:ℚ, bᵈ ≠ g` comes from "g not a perfect `d`-th power"
  via integral closure (`no_rat_dth_root`, using `IsIntegrallyClosed ℤ`). Memories
  `[[lean-isintegrallyclosed-int-via-rationalroot]]`, `[[lean-kummer-irreducibility-locations]]`.
- **The base-2 `binary_floor_eq` generalized cleanly to `gary_floor_eq`** (base-`g` block orbit recovers
  `⌊W·gⁿ⌋`): digits `{0,…,g-1}`, geometric bound `∑(g-1)·g^{-(j+1)} = 1`, strict via a tail digit `≤ g-2`.
- **Headline form differs from base-2's** in encoding the digit: base-2 uses `x = 0 ∨ x = 1`; base-`g`
  uses the interval `0 ≤ x ≤ g-1`. They coincide at `g=2` because `x` is an integer (`dStepZ`) — that's
  exactly the `BaseGSubsumes` cross-check.
- **Why no Aristotle this lap**: nothing was *genuinely open that I wasn't already closing locally* — the
  whole port was mechanical mirroring + clean mathlib lemmas. Idle Aristotle was correct.

## ✅ State (all axiom-clean, build 🟢 8304; 12 commits this lap)
- **Analytic** (`BaseGWeyl`, `BaseGEquidist`): `g_pow_inj`, `baseG_weyl_L2_mean/_norm/_normalized`,
  `fourier_baseG_eq`, `ae_baseG_weyl_tendsto(_real)`, `ae_baseG_orbit_equidistributed(_real)`.
- **Torus density** (`BaseGTorusEquidist`): `dTorusOrbitG`, `ae_W_dTorusG_orbit_dense` (hyp form) +
  `_eisenstein` / `_prime` / `_odd` discharge variants; `dXiG_ne_zero{,_prime,_odd}`.
- **Lin-indep** (`RpowLinIndep`): `rpow_lin_indep_int_base` (Eisenstein), `no_rat_dth_root`,
  `not_perfect_pow_of_lt`, `rpow_lin_indep_int_prime` (all `g`, prime `d`), `rpow_lin_indep_int_odd`
  (odd composite `d`).
- **Assembly** (`BaseGFinish`): `dGpdTorusG_orbit`, `exists_exceeding_torus_point_base`,
  **`ae_no_dStep_schedule_reads_base_g`** (Eisenstein) + **`_all`** (every `g`, prime `d`),
  `grt_lt_bound`, the three capstones `ae_not_DStep{DigitRepresentable,ReadsBaseG,Recurrence…}BaseG`
  (Eisenstein) + **`_all`** (every `g`); concrete unconditional instances
  `ae_no_dStep_schedule_reads_base_three` (`g=3`) and `base_four` (`g=4=2²`, perfect power).
- **Orbit bridge** (`GeneralOrbit`): `dStepF_orbit_real`/`dStepF_eq_orbitF_real`/`dStepPartial_eq_dGpd_real`.
- **Window** (`RpowWindow`): `exists_scale_outside_window_strict_base` (width-`b`).
- **Subsumption** (`BaseGSubsumes`): `grt_two_eq_rrt`, `baseG_two_digit_iff`,
  `DStepDigitRepresentableBaseG_two_iff`, `ae_not_DStepDigitRepresentableBaseG_two_via_base_two`.
- **`gary_floor_eq`** (`GaryExpansion`).

## 🎬 Next actions
The base-`g` frontier is essentially closed. Pick the highest-value remaining thread:
1. **`_odd` headline + capstones** (mechanical): thread `ae_W_dTorusG_orbit_dense_odd` into a headline
   mirroring `ae_no_dStep_schedule_reads_base_g_all`, plus an odd-composite concrete instance (e.g. `g=2,
   d=9`? — check window bound `2^{1/9} < 4/3` holds, and `2` not a perfect-`p` power for `p∣9` i.e. `p=3`,
   `2` not a cube ✓). Cleanest after refactoring the headline proof to take the density a.e.-statement as a
   parameter (kills the current `_all`/Eisenstein/`_odd` triplication).
2. **Even composite-`d` for perfect-power `g`**: needs the `p=2` prime-power Kummer case (mathlib TODO) or
   the `4∤n` analysis — niche, since a prime `d` always suffices for the headline.
3. **Fixed-`W` version** — OPEN MATH (Borel normality of a specific number; cf. `{(3/2)ⁿ}` not even known
   dense). Not attemptable in mathlib; a literature lap at best.

`PENDING_WORK.md` top (★★★★★) has the full state and the precise routes. The reference corpus is at
`~/personal/claude/knowledge/core/projects/lean-journey/reference/` (read-only; `ls` + `grep -rl` before
re-deriving friction).

## ⚠️ Gotchas
- Slim import sets: `IsIntegrallyClosed ℤ` needs `import Mathlib.RingTheory.Polynomial.RationalRoot`; the
  odd/prime-power Kummer lemmas need `import Mathlib.FieldTheory.KummerExtension` (prime one is in
  `KummerPolynomial`).
- `Nat.Prime.prime_int` doesn't exist; use `Nat.prime_iff_prime_int.mp`.
- New files MUST be wired into `Erdos482General.lean` or the pre-commit `lake build` gate won't check them
  (it builds the aggregator). All this lap's files are wired in.
- Pre-existing deprecation warnings (`push_neg`, unused `simp` args) in `Equidistribution`/`MultidimWeyl`/
  `EquidistDense`/`GeneralTorusFinish` are noise, not errors.

## 📁 Key files
- `src/Erdos482/General/BaseGFinish.lean` — headlines + capstones + concrete `g=3,4` instances.
- `src/Erdos482/General/{BaseGWeyl,BaseGEquidist,BaseGTorusEquidist}.lean` — the analytic engine + density.
- `src/Erdos482/General/RpowLinIndep.lean` — all the lin-indep bricks (Eisenstein / prime / odd).
- `src/Erdos482/General/{GaryExpansion,BaseGSubsumes}.lean` — base-`g` floor recovery + g=2 cross-check.
- `PENDING_WORK.md` (top ★★★★★) — authoritative state.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor, don't wait for
instructions, and don't offer other KB projects — this doc IS the chosen thread. The base-`g`
general-degree impossibility is COMPLETE for every base `g ≥ 2` (headline + capstones, prime & odd degrees,
axiom-clean). Absorb the context above, then pick up at "Next actions" #1 — the `_odd` headline + capstones
(ideally via the density-parameter refactor that de-duplicates the headline) — and start working.**
