# Handoff: cubic Tier-1 refuted + Tier-2 scaffold (defect identity → conditional impossibility)

**Date**: 2026-06-14 04:42 UTC · **Branch**: `st06` · **HEAD**: `a7954c3` (latest Lean commit `cc2218b`)

## 🎯 What we're doing
Formalizing Stoll [St06] in Lean 4 — the "for fun" extension of the (complete, axiom-clean on `main`)
Erdős #482 / Graham–Pollak work. St06's main theorems are all done (Thms 3.1/3.3/3.4, Cor 3.5,
`SelfRefWall`). The live thread is the **cubic self-referential frontier**: does the `√2`/base-2
digit-extraction miracle have a `2^{1/3}` analogue?

## 🧠 What happened this lap (all observed; build 🟢 8275 jobs, every new decl `#print axioms` = trust base)

**1. Refuted the prior findings doc's "Tier-1" cubic impossibility (a real math catch).** The
harvested `ON-LINE-FINDINGS-…cubic-selfref-literature.md` recommended proving that *no constant offset
`c`* makes the single-internal-floor crux `0 ≤ {x} − 2^{1/3}·{x/2} + c·2^{1/3} < 1` hold ∀x.
**That is FALSE — `c=½` works.** HOSTCHECK (2M samples) + Lean both confirm. New in
`SelfRefWall.lean` (axiom-clean):
  - `onefloor_div2_crux_solvable` — `c=½` works for **every** multiplier `0 ≤ β < 2`;
  - `onefloor_div2_crux_cbrt2` — explicit `β=2^{1/3}` instance (the refutation);
  - `onefloor_div2_crux_solvable_iff` — sharp: solvable ⇔ `β<2` (witnesses `x=0`,`x=1`; `β=2` breaks);
  - `onefloor_div2_offset_unique` — `c=½` forced for `0<β<2`.
  Why the two-witness method scales for `selfref_crux_fails_of_three_le` (division `/g` AND multiplier
  `√g` both grow) but NOT here (division fixed at base 2, only multiplier changes): the range of
  `{x}−β{x/2}` is width-exactly-1 and fits any window for `β<2`. **The cubic obstruction is NOT at the
  single-floor level.**

**2. Built the Tier-2 scaffold — `src/Erdos482/General/CubicDefect.lean` (NEW, wired in, axiom-clean):**
  - `cubic_threestep_defect` — the exact algebraic identity (pure algebra from `α³=2`): the 3-step map
    `v₃=⌊α(⌊α(⌊α(u+c₀)⌋+c₁)⌋+c₂)⌋` satisfies `v₃ = 2u + (2c₀+α²c₁+αc₂) − (α²f₁+αf₂+f₃)`. So the digit is
    `v₃−2u = C − defect`, `C:=2c₀+α²c₁+αc₂`, `defect:=α²f₁+αf₂+f₃`.
  - `cubic_combined_defect_range_wide(_cbrt2)` — the combined two-floor defect spans width `α²+α+1>1`,
    so it fits **no** width-1 window. Two internal floors ⇒ spread >1 (vs single floor's spread =1).
  - `cubicV3`, `cubicDefect`, `cubicV3_sub_eq` — orbit machinery + the digit=C−defect bridge.
  - `cubic_threestep_digit_pair_fails` — **THE CONDITIONAL IMPOSSIBILITY (honest ceiling):** if the orbit
    realises two starts `u,u'` whose combined defects differ by >1, the two digits can't both be in
    `{0,1}`. So no fixed schedule reads base-2 digits along an orbit exploring a wide defect pair.
  - `cubic_valid_digits_defects_close` — positive form: valid digits ⟹ all orbit defects within a
    width-1 window. The clean statement to chain a future equidistribution lemma against.
  - `irrational_cbrt_two` — `2^{1/3}` is irrational (base prerequisite for any equidistribution route).

**3. Corrected an over-claim & mapped the residual.** The unconditional cubic impossibility hinges on
equidistribution of `{α^n ξ}` for a **fixed** `ξ` — which is **OPEN** (cf. `{(3/2)^n}`), NOT "guaranteed
because α is non-Pisot" as the findings doc said (that conflates the a.e.-`ξ` Weyl/Koksma theorem with
the fixed-`ξ` question). Filed `ON-LINE-REQUEST.md` to confirm. Numerics (`tools/sandbox/
cubic_uniform_escape_probe.py`): float64 "survival caps ~52" is a **2⁵³-precision ARTIFACT**; exact
`Decimal`(400)+`Fraction` shows the doc triple fails at block **63**, and an exact local search found a
schedule surviving **≥91** (multi-seed exact hill-climbing converges to exactly 91) — so "sup~64" was
wrong and survival leans toward unbounded (⇒ the open equidistribution is genuinely needed; attack-path
#3 likely dead, but UNRESOLVED — hill-climbing only lower-bounds `sup N`; needs constraint-propagation.
See PENDING_WORK ★).

## 🔁 POST-0442 ADDENDUM (same lap, later)
- **`cubic_block_orbit_base_two_bounds`** (NEW, axiom-clean) — proves the block orbit is `uₙ=⌊W·2ⁿ⌋`
  (doubles per block, `u_{n+1}=2uₙ+dₙ`). **This CORRECTS my own `{α^n ξ}` framing:** the orbit is base 2,
  so `f₁≈{αW·2ⁿ}` is a **doubling-map** orbit ⇒ the residual wall is **base-2 normality of `αW`**, not
  geometric equidistribution. HOSTCHECK: `uₙ/2ⁿ→1.24987=W`.
- **Harvested** `ON-LINE-FINDINGS-…equidistribution-xi-theta-n.md` → `archive/findings/`. It independently
  confirms my catch: Koksma 1935 = a.e.-`ξ` theorem; fixed-`ξ` OPEN (cf. `{(3/2)ⁿ}`); prior doc
  over-claimed; mathlib lacks geometric `{ξθⁿ}`.
- **Tractability nuance for path #2:** the base-2 reframing means the needed map is `×2` (the doubling
  map), and mathlib HAS `×n` endomorphism ergodicity (`AddCircle.ergodic_nsmul`) — unlike the absent
  geometric `×2^{1/3}`. **BUT** I checked: Birkhoff's *pointwise* ergodic theorem is NOT in mathlib
  (`Dynamics/BirkhoffSum/*` is only sum-definitions), so path #2 still needs Birkhoff built. Net: path #2
  improved from "two missing pieces + exotic map" to "one missing piece (Birkhoff) + standard map", still
  large. Conditional theorem (`cubic_threestep_digit_pair_fails`) remains the formalizable ceiling.

## ✅ Aristotle (keep ONE in flight)
- `onefloor_unique` (`1bd5fc63`) ✅ COMPLETE — independently proved `onefloor_div2_offset_unique`,
  axiom-clean, matches local proof.
- `cubicdefect` (`61f40723`) ✅ COMPLETE — independently confirmed `cubic_threestep_defect` (its log:
  "proof complete, axiom-clean"). Strong faithfulness signal for the centerpiece identity.
- `cubicpairfail` (`4a8f9e74`) ⏳ RUNNING — cross-check of the conditional impossibility core.

## 🎬 Next actions
1. `aristotle list` (the one-shot, NOT the live TUI which hangs) → when `cubicpairfail` (`4a8f9e74`)
   returns, it cross-checks the conditional theorem; verify, then submit the next bounded lemma to keep
   one in flight.
2. **Decide path #3 (highest leverage):** run a *principled* exact-arithmetic search (continuation /
   constraint-propagation on the offset-polytope, NOT random hill-climbing) using
   `tools/sandbox/cubic_uniform_escape_probe.py::first_fail_exact`. If `sup N < ∞` → finite decidable
   unconditional proof (the win); if unbounded → the conditional theorem is the ceiling, stop here.
3. If `ON-LINE-FINDINGS-*equidistribution*` appears, harvest it; it confirms whether unconditional Tier-2
   is beyond current math. Then frame STATUS/notes as "cubic open in mathematics, conditional formalized."
4. Lower-priority Lean: formalize `1,α,α²` ℚ-linearly-independent (irreducibility of `X³−2`) as a
   prerequisite for attack-path #2 (almost-all-`W` version).

## ⚠️ Gotchas
- DO NOT push (Trevor merges); commit every green build. Pre-commit runs full `lake build` (slow) and
  fires on `.lean` under `tools/aristotle/` even though they aren't build targets.
- `#print axioms` clean ≠ statement-faithful (the load-bearing lesson). This lap's refutation of "Tier-1"
  is a fresh example: a plausible cited claim was just wrong. Numerically verify cubic claims before
  trusting (and beware float `2⁵³` artifacts — use the exact `Decimal`+`Fraction` probe past block ~45).

## 📁 Key files
- `src/Erdos482/General/CubicDefect.lean` — the Tier-2 scaffold (NEW).
- `src/Erdos482/General/SelfRefWall.lean` — single-floor solvability suite + the Tier-1 refutation.
- `notes/CUBIC-EXPLORATION.md` (2026-06-14 update) · `PENDING_WORK.md` (★ ACTIVE FRONTIER) ·
  `tools/sandbox/cubic_uniform_escape_probe.py` (float-artifact caveat + `first_fail_exact`) ·
  `STATUS.md` (axiom ledger) · `ON-LINE-REQUEST.md` (equidistribution-openness) ·
  `archive/findings/ON-LINE-FINDINGS-2026-06-13-cubic-selfref-literature.md` (harvested).

---
**→ Next session: this is your starting point. Pick up at "Next actions" #1, keep one Aristotle job in
flight, and drive the cubic frontier. Don't re-trust "Tier-N" recommendations without a numeric check.**
