# Handoff: Erdős #482 — Theorem 3.2 complete for 7/8 pairs (full intervals); pair 5 = the Diophantine thread

**Date**: 2026-06-06 21:05 UTC (updated 21:50) · **Branch**: main · **HEAD**: `0608e02`

## 🎯 What we're doing
Formalize Stoll's *A fancy way to obtain the binary digits of 759250125√2* (arXiv:0902.4168). Headline
(digits of √2 = Graham–Pollak) and bonus (Theorem 3.2 + Cor 3.3) were already complete & axiom-clean.
This lap pushed the bonus to **verbatim faithfulness and near-completeness**.

## ✅ State (all axiom-clean, `lake build` green = 8255 jobs, verified via pre-commit gate every commit)
This lap added (all `#print axioms` = trust base only, grep-verified zero sorry/zero custom axiom):
- **`stoll_pair6` + `stoll_pair6_t`** — pair 6 for its **FULL ε-interval** (was only the Cor-3.3 ε).
  This **completes Theorem 3.2 for all 7 non-special pairs over their whole intervals.** Engine:
  `cor33_base_interval` (62-step base case; the 2 endpoint-defining tight steps 30/58 close by exact
  √2-coefficient cancellation — see the flushed memory `lean-exact-endpoint-tight-step-cancellation`).
- **`stoll_pair{1,2,3,4,7,8}_t` + `cor33_unconditional_t`** — verbatim `tᵢ`-form (digits of
  `tᵢ=(αᵢ√2−βᵢ)/2^lᵢ`, not just `αᵢ√2`), via the existing `binDigit_div_pow`.
- **`stoll_endpoints_strictMono` + `stoll_intervals_cover`** — the 8 intervals are disjoint & exactly
  cover `[1−√2/2, √2/2)` (completeness of Thm 3.2 over the admissible range).
- **`stoll_gp_isBit`** — master theorem: GP difference ∈ {0,1} for ε in any of the 7 proven intervals,
  k≥31 (case-split + omega re-indexing + `binDigit_mem_zero_one`).
- **`vv_one_le_and_mono`** — vv ≥1 and weakly monotone (Aristotle-proved job `135bea06`, kernel-verified).
- **`vv_half_eq_u` + `stoll_pair5_half`** — `vv (1/2) = u`, so pair 5 at ε=½ IS the headline.

**Late additions (post-2105, pair-5 progress):**
- **`vv_eq_u_of_evenstep`** + **`stoll_pair5_of_evenstep`** — pair 5 reduced to ONE clean hypothesis
  `Heven : ∀ j, ⌊√2·(u(2j)+ε)⌋₊ = u(2j+1)`. Given `Heven`, `vv ε = u` and pair 5 follows from
  `graham_pollak`. Axiom-free. This isolates pair 5's *entire* difficulty into `Heven`.
- **`sqrt2_badly_approximable`** — `1/(3q) ≤ |q√2 − p|` (√2's explicit irrationality-measure-2 bound),
  the Diophantine lever needed to discharge `Heven`. Aristotle job `b58583f6`, kernel-verified clean.

13 commits, `10d3903`..`19704b3`. Stoll.lean now 29 theorems, ~1510 lines.

## 🧠 Context to carry forward
- **The only remaining math gap is pair 5's full interval** (`t₅=√2`, β=0). **⚠️ My initial model of it
  was WRONG — read this whole bullet before touching pair 5** (full detail in `PENDING_WORK.md §1`):
  - **Superseded (do not act on)**: I first found `vv ε = u` on the claimed interval `[309/2√2−218,
    1296121037/2√2−916495974)` "for all n" — verified only to n=300 — and reduced pair 5 to
    `Heven : ∀ j, ⌊√2(u(2j)+ε)⌋₊ = u(2j+1)`.
  - **CORRECTION (verified far out)**: for the interior midpoint, `vv ε` diverges from `u` at **n=905**
    and the digit-difference first fails √2's digit at **n=452**. So interior ε do NOT reproduce √2's
    digits at large n; `Heven (∀ j)` holds essentially only at ε=½. **The `vv = u` path is a DEAD END.**
  - **Implication**: either the pair-5 interval is wrong/misread (the transcribed pair-5 eqs were
    already flagged suspect — predicted `vv3=2`, true `4`), or the valid ε-set is subtler than a full
    interval (possibly Cantor-like), or the digit index has an unpinned phase. **Don't formalize pair 5
    against the current interval until the paper's actual claim is confirmed.**
  - The committed `vv_eq_u_of_evenstep`, `stoll_pair5_of_evenstep`, `sqrt2_badly_approximable` are all
    CORRECT, axiom-clean — but the first two only cover ε=½ now; keep them as infra.
- **Why I stopped**: pair 5 is blocked on the `ON-LINE-REQUEST` for Stoll's actual §4 argument; the
  Aristotle Diophantine job `b58583f6` already returned and is harvested (→ `sqrt2_badly_approximable`,
  committed & verified). I deliberately did NOT add a half-done pair-5-with-axiom (would regress the
  clean zero-axiom state).

## 🎬 Next actions
**⚠️ FIRST READ `PENDING_WORK.md §1` — the `vv = u` / `Heven` pair-5 model is a confirmed DEAD END.**
Late-lap numerics: for interior ε in the claimed pair-5 interval, `vv ε` diverges from `u` at n=905
and the digit-difference fails √2 at n=452. So either the interval is wrong (transcription was flagged
suspect) or the valid ε-set is subtler than an interval. **Do not formalize pair 5 against the current
interval — it may be incorrect.**
1. **Resolve what Stoll actually proves for pair 5.** Check the repo root for an `ON-LINE-FINDINGS-*.md`
   answering the `ON-LINE-REQUEST.md` pair-5 item (read it, act, `git mv` to `archive/findings/`). If
   absent, the request is still open — work other angles, do NOT guess the interval.
2. **Re-examine the paper's Remark (b)** ("digits of √2 for any ε ∈ [0.4959953…, 0.5012400…)") against
   the n=452 divergence numeric — reconcile (precision was ruled out; prec=3000 ≫ needed). Likely the
   digit *index* is offset, or "obtained for" means something weaker than I assumed.
3. The committed `vv_eq_u_of_evenstep` / `stoll_pair5_of_evenstep` / `sqrt2_badly_approximable` are all
   CORRECT axiom-clean theorems — keep them (the first two cover ε=½; the third is a genuinely useful
   √2 Diophantine bound). They're just not (yet) the route to the full interval.
4. If pair 5 stays unresolved, the formalization of **Theorem 3.2 for 7/8 pairs + Cor 3.3 is already a
   complete, faithful, axiom-clean result** — a fine stopping point to write up. Consider whether pair 5
   is worth further effort vs. declaring the 7-pair version the deliverable (Trevor's call).

## ⚠️ Gotchas
- `cor33_base_interval`, `cor33_base_of_bounds`, `stoll_pair7_base` carry `set_option maxHeartbeats
  1000000`/`4000000` — keep them. Base cases are **script-generated** (Python loop), don't hand-edit.
- mathlib v4.29.1: `lt_or_ge` (NOT `lt_or_le`); `ite_self` (NOT `if_t_t`); `pow_le_pow_left₀`;
  `Real.pi_gt_d6`/`pi_lt_d6`; `Real.exp_one_gt_d9`/`lt_d9`.
- Stoll.lean now imports `Erdos482.Digits` and `Erdos482.Main` (added this lap for
  `binDigit_mem_zero_one` and `graham_pollak`). No import cycle.
- Numerics first, always — pair-5's whole story came from Python before any Lean.

## 📁 Key files
- `src/Erdos482/Stoll.lean` — the entire bonus (~1460 lines). All this lap's work is here.
- `PENDING_WORK.md` — **read §1 first**: the sharp pair-5 characterization + 4 attack paths.
- `STATUS.md` — refreshed this lap (axiom ledger, what's-happened).
- `ON-LINE-REQUEST.md` — open ask for Stoll's pair-5 proof.
- `archive/findings/ON-LINE-FINDINGS-2026-06-06-stoll-thm32-cor33.md` — paper transcription (pair-5 eqs
  are SUSPECT, don't trust them).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. Start at
Next actions #1: read `PENDING_WORK.md §1` (the `vv = u` pair-5 model is a confirmed DEAD END), then
check for an `ON-LINE-FINDINGS` doc and resolve what Stoll actually claims for pair 5 BEFORE any Lean.
The rest of Theorem 3.2 (7/8 pairs, full intervals) + Cor 3.3 is done & axiom-clean — a fine deliverable
on its own if pair 5 stays blocked.**
