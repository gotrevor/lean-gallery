# Handoff: cubic path-#2 — steps (a)+(b)+(c).1+(c).2 DONE (a.e.-W backbone built)

**Date**: 2026-06-14 07:19 UTC · **Branch**: `st06` · **HEAD**: `3cb155f`

## 🎯 What we're doing
Formalizing Stoll [St06] in Lean 4 — a "for-fun" extension of the complete, axiom-clean Erdős #482
work. All #482/St05/St06 main theorems are done & axiom-clean. The **live thread is the cubic
self-referential frontier**: building an **unconditional a.e.-`W` impossibility** for the `2^{1/3}`
analogue of the √2 base-2 digit miracle (attack-path #2 in `PENDING_WORK.md ★★`). This lap built the
ENTIRE analytic backbone of that route; what remains is the geometry-specific finish.

## 🧠 Context to carry forward (read this — supersedes prior pessimism)
- **The route, precisely.** A cubic digit-reading orbit pins the combined floor-defect
  `α²f₁+αf₂+f₃` to the 2-point set `{C,C−1}` (`CubicDefect.cubic_orbit_defect_confined`). The `fᵢ` are
  functions of the **base-2** doubling orbit `2ⁿ(W,αW,α²W) mod 1 ∈ T³` (block orbit `uₙ=⌊W2ⁿ⌋` — base-2,
  NOT geometric `(2^{1/3})ⁿ`; that reframing is the key insight). If that T³ orbit is **dense** for a.e.
  `W`, the defect spans width `α²+α+1>1` (`cubic_combined_defect_range_wide`) ⇒ escapes `{C,C−1}` ⇒ no
  schedule reads a.e. `W`'s digits. **a.e.-`W` is buildable; specific-`W` is open math** (Borel
  normality of a self-defined constant — don't chase it).
- **By Weyl** (the reduction): T³ equidistribution ⟺ ∀`(a,b,c)≠0`, `(1/N)∑e(2πi·2ⁿ·ξW)→0`,
  `ξ=a+bα+cα²`. `cubic_lin_indep_int` gives `ξ≠0`, so this is the **1-D** doubling-Weyl sum at `s=ξW`.
- **What got BUILT this lap (all axiom-clean, build 🟢 8281):** the whole chain except the final
  geometry. Step (a) DEL engine, step (b) full a.e. doubling equidistribution, step (c) pieces 1+2
  (scaling transfer + multidim Weyl criterion). See State.
- **Big de-risking discovery:** mathlib HAS `Mathlib.Analysis.Fourier.AddCircleMulti` — `mFourier`
  (torus characters) + `span_mFourier_closure_eq_top` (torus Stone–Weierstrass). So the multidim Weyl
  criterion was a near-mirror of the 1-D one (done), NOT a from-scratch build.
- **Abandoned/avoided:** path #3 (finite uniform escape) — exact search found survival ≥91 blocks ⇒
  leans `sup N=∞` ⇒ equidistribution genuinely required (don't re-run numerics). The geometric
  `{ξθⁿ}` framing (superseded by base-2). Density-lemma-only closes (need equidistribution level).
- **whnf gotcha (cost me time, now flushed to KB):** matching a finite `∑` with cast-laden summands
  (`((k*2ⁿ:ℤ):ℂ)`) against an abstract lemma whnf-loops; **`set a := <summand-lambda>` first** so
  unification stays opaque (used in `DoublingEquidist`). And `rw [show TARGET from fubini_lemma]` to
  coerce the dependent-pi/redex Fubini lemma (used in `MultidimWeyl.integral_mFourier_eq`).

## ✅ State (axiom-clean = `[propext, Classical.choice, Quot.sound]`; full `lake build` 🟢 8281 jobs, observed)
New axiom-clean decls this lap (`General/Equidistribution.lean`, `DELEngine.lean`, `DoublingEquidist.lean`,
`MultidimWeyl.lean`; all wired into `Erdos482General.lean`):
- **`weyl_criterion`** — 1-D Weyl equidistribution criterion (Stone–Weierstrass via
  `span_fourier_closure_eq_top` + `norm_cesaro_le`); `IsEquidistributed`, `integral_fourier_eq`.
- **`ae_tendsto_zero_of_summable_sq`** — DEL L² engine (Markov+Borel–Cantelli; Aristotle `bd44d316`).
- **`cesaro_fill_of_subseq_sq`** (gap-fill `j²`→all `N`), `l2_bridge` (Bochner↔lintegral; `190d0b98`),
  `tsum_ofReal_inv_sq_ne_top` (p-series), `fourier_doubling_eq` + `norm_doubling_exp`.
- **`ae_doubling_orbit_equidistributed`** — STEP (b): a.e. `s∈[0,1]`, `{2ⁿs}` equidistributes on `ℝ/ℤ`
  (Borel base-2 normality, DEL-built). Via `ae_doubling_weyl_tendsto` (per-`k`).
- **`integral_mFourier_eq`** (`∫ mFourier n=δ`) + **`weyl_criterion_torus`** (+`IsEquidistributedTorus`)
  — STEP (c) piece 2: multidim Weyl criterion on `Tᵈ`.
- **`ae_comp_mul_left`** — STEP (c) piece 1: a.e. transfer under nonzero scaling (Aristotle `10ed15fc`).
- `norm_cesaro_le` generalized to any compact domain (serves 1-D + torus).
- **Aristotle:** `3e68d32f` (equidense) RUNNING = `IsEquidistributed ⇒ Dense (range x)` (general,
  compact + finite open-pos measure), the key sub-lemma for piece 3.

## 🎬 Next actions
1. `aristotle list` → harvest **`equidense` (`3e68d32f`)** when IDLE; verify in-kernel + `#print axioms`,
   port (probably into `MultidimWeyl.lean`), specialize to the torus `volume`. Then submit the next open
   lemma (keep one in flight) — good candidate: the **periodicity bridge** (#2 below) or a piece-3 sub-lemma.
2. **Periodicity bridge** (small glue, needed before scaling): step (b)/`ae_doubling_weyl_tendsto` is over
   `volume.restrict (Icc 0 1)`; `ae_comp_mul_left` needs **full `volume` ℝ**. The doubling Weyl sum is
   1-periodic in `s` (`e(2πi k 2ⁿ(s+1))=e(…)`), so a.e.-[0,1] ⟹ a.e.-ℝ. Prove via the bad set being a
   ℤ-periodic null set: `volume(B)=∑_m volume(B∩[m,m+1])=∑_m volume((B−m)∩[0,1])=0` (translation-invariance
   + periodicity + countable cover — cf. the cover in `DoublingOrbit.ae_fract_dense_real`). Good Aristotle target.
3. **Final assembly → a.e.-`W` T³ equidistribution:** for each `n:Fin 3→ℤ`, `n≠0`, set `ξ=∑nᵢαⁱ⁻¹`
   (`≠0` by `cubic_lin_indep_int`); the character Cesàro mean `(1/N)∑ mFourier n (2ᵏ(W,αW,α²W))` equals
   `(1/N)∑e(2πi·2ᵏ·ξW)` = the frequency-1 doubling sum at `s=ξW` (= `ae_doubling_weyl_tendsto 1` scaled
   by `ae_comp_mul_left` after #2). Intersect over `n≠0` (`ae_all_iff`) ⇒ `weyl_criterion_torus` ⇒ a.e.
   `W`, `2ᵏ(W,αW,α²W)` is `IsEquidistributedTorus`. (Need: `mFourier n (orbit pt) = e(2πi 2ᵏ ξW)` — a
   torus analogue of `fourier_doubling_eq`, and the orbit `2ᵏ(W,αW,α²W) mod 1` expressed in `T³`.)
4. **Piece 3 (CubicDefect link, the geometry finish):** a.e.-`W` T³ equidist ⇒ (via `equidense`) dense ⇒
   express `(f₁,f₂,f₃)` as functions of the `T³` orbit point and show the defect range exceeds width 1
   (`cubic_combined_defect_range_wide`) ⇒ contradicts `cubic_orbit_defect_confined` ⇒ **unconditional
   a.e.-`W` cubic impossibility**. This is the genuinely CubicDefect-specific part — read `CubicDefect.lean`
   carefully to wire the `fᵢ`-as-orbit-functions step.

## ⚠️ Gotchas
- DO NOT push (Trevor merges). Commit every green build; pre-commit runs full `lake build` on `.lean`
  changes (skips doc-only). Verify green from real output before committing — done this lap.
- `set a := <cast-laden summand lambda>` to dodge the whnf loop when feeding a sum to an abstract lemma.
- Fubini-pi `rw` needs `rw [show TARGET from integral_fintype_prod_volume_eq_prod …]` (redex/dependent-pi).
- `#print axioms` clean ≠ statement-faithful — Aristotle caught the vacuous `Summable(∫⁻‖·‖₊²)` over ℝ≥0∞.
- `squeeze_zero_norm`'s bound arg is `a` (not `g`); `tendsto_const_nhds` via `show … from`, not `(a:=)`.

## 📁 Key files
- `src/Erdos482/General/Equidistribution.lean` — gap-fill, 1-D `weyl_criterion`, `fourier_doubling_eq`,
  `norm_cesaro_le` (generic), p-series, `integral_fourier_eq`, `IsEquidistributed`.
- `src/Erdos482/General/DELEngine.lean` (`import Mathlib`) — DEL engine, `l2_bridge`, `ae_comp_mul_left`.
- `src/Erdos482/General/DoublingEquidist.lean` — STEP (b) `ae_doubling_orbit_equidistributed`.
- `src/Erdos482/General/MultidimWeyl.lean` — STEP (c).2 `weyl_criterion_torus`, `integral_mFourier_eq`.
- `src/Erdos482/General/CubicDefect.lean` — the geometry (defect confinement, lin-indep) for piece 3/4.
- `PENDING_WORK.md ★★` (authoritative (a)→(b)→(c) roadmap, status-marked) · `STATUS.md` (ledger).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor, don't wait for
instructions, and don't offer other KB projects — this doc IS the chosen thread. Absorb the context
(esp. the base-2 reframing, the Weyl→1-D reduction, and the whnf/Fubini gotchas), then pick up at Next
action #1: `aristotle list` → harvest `equidense` (`3e68d32f`), then drive the periodicity bridge +
final assembly toward the a.e.-`W` cubic impossibility.**
