# Handoff: St06 COMPLETE — Cor 3.5 capstone + Thm 3.4 full-interval obstruction, all axiom-clean

**Date**: 2026-06-13 ~22:40 UTC · **Branch**: `st06` (the "for fun" St06 extension; #482 is done on `main`)

## 🎯 What happened this lap (review lap → two major closures)
Both remaining open St06 items are now CLOSED and axiom-clean. **St06 has no open mathematical items.**

1. **Cor 3.5 — the Beatty-unification capstone — DONE without the PDF.** The handoff's #2 ("PDF-blocked
   index convention") was illusory. Reverse-engineered the statement numerically: the GP recurrence
   `su √2 √2 ½ ½ m` started at the Beatty value `m = ⌊r·α⌋` (`α∈{1+√2, 1+1/√2}`) reads off the binary
   digits of **`r·√2`**. Stoll's printed `w`-table is just a mantissa renormalization of the same digit
   string. Built (`src/Erdos482/General/St06Cor35.lean`):
   - engine `cor35_pair` / `cor35_pair_case2` = the classical `gp_pair` generalized by a free factor `r`
     (`cor35_floorA`/`cor35_floorB`/`cor35_base`, all from the universal `crux`/`eq8_general`);
   - `cor35_digits_case1`/`case2`; Beatty bridges `beatty_start_case1`/`case2`;
   - **capstone `st06_cor35`** (case-split on `beatty_unique_sqrt2`); literal `st06_cor35_realDigits`
     (= `Real.digits (r√2) 2 j`), `st06_cor35_isBit`, faithfulness `st06_cor35_recovers_gp` (r=1 → √2)
     and `binDigit_three_sqrt2_first_four` (r=3 → 3√2 = …0,0,1,1₂, a genuinely new expansion).
2. **Thm 3.4 full interval — RESOLVED like pair 5** (`src/Erdos482/General/St06Thm34.lean`):
   `st06_thm34_bstep_value` (exact general-ε b-step), `st06_thm34_bstep_band` (lands iff
   `frac∈(−d,1−d]`, cf. `pair5_estep_band`), and `st06_thm34_band_fails_below_half`/`_above_half` — a
   **machine-checked Diophantine obstruction**: no single ε≠½ extracts digits for every `w`. So Stoll's
   printed k-dependent interval is NOT a t-universal theorem; ε=½ (`st06_thm34_digits`) is the proven ceiling.

Plus: comprehensive `#print axioms` sweep (every St06 + main headline = trust base only, 0 math axioms);
a **cubic exploration** (`notes/CUBIC-EXPLORATION.md`) — a negative result: the self-referential digit
trick does NOT extend to cubic irrationals (α=2^{1/3}; the 3-step bitstream breaks at j=64), genuinely
"needs new math", not a formalization gap.

## ✅ State
- **Build 🟢 green (8273 jobs)** — observed from full `lake build`. HEAD will be the latest `st06` commit.
- Everything axiom-clean (`[propext, Classical.choice, Quot.sound]`). Zero `sorry`, zero custom axiom.
- **Aristotle**: all three submitted lemmas cross-validated my local proofs (engine `cor35_pair`,
  `st06_thm34_bstep_value`, `st06_thm34_bstep_band` — each from the stated axioms only). One job in
  flight: `cor35cap` (`5d48c0b1-fb0f-4a50-80b9-820a17ae9b31`) cross-validating the capstone case-split.

## 🎬 Next actions (all OPTIONAL polish — no open math)
1. Poll `aristotle list` for `5d48c0b1`; if returned, `aristotle show` it (confirmatory only).
2. Optional: wire `st06_cor35` + `erdos482_resolution` into one top-level St06 showcase module; a unified
   Thm 3.3/3.4/Cor 3.5 `isBit` master.
3. Curiosity (non-blocking, `ON-LINE-REQUEST.md`): does Stoll's *printed* Thm 3.4 interval claim a
   per-`w` Diophantine result, or is it a transcription artifact? Either answer leaves our content intact.
4. Genuinely-open research (multi-lap, "needs new math"): self-referential digit recurrences for
   non-quadratic algebraic numbers (see `notes/CUBIC-EXPLORATION.md` for the mapped wall).

## ⚠️ Gotchas
- DO NOT push (Trevor merges); commit every green build. Pre-commit runs `lake build`.
- `St06Cor35.lean` now imports `Erdos482.Induction` + `Erdos482.Digits` (for `crux`/`eq8_general`/
  `binDigit`/`digits_eq_floor_sub`/`binDigit_mem_zero_one`).
- The engine = `gp_pair` with a factor `r`; `crux`/`eq8_general` are universal so insertion is mechanical.

## 📁 Key files
- `src/Erdos482/General/St06Cor35.lean` (capstone), `St06Thm34.lean` (ε=½ + obstruction).
- `notes/ST06-THM34-FINDINGS.md` (Thm 3.4 resolution), `notes/CUBIC-EXPLORATION.md` (negative result).
- `tools/sandbox/st06_cor35_*.py`, `st06_thm34_band.py`, `cubic_*.py` (ground-truth numerics).
- `STATUS.md` (living overview, refreshed this lap), `PENDING_WORK.md` §0′.

---
**→ Next session: St06 is complete & axiom-clean. This is a polish/maintenance baton — there is no open
obligation. Pick an optional item above, or (with the operator) decide whether St06 is "done". Keep one
Aristotle job in flight. Do NOT chase Stoll's printed Thm 3.4 interval as a theorem — it is provably not
t-universal (the obstruction is in the repo).**
