# Handoff: cubic path-#2 DEL infrastructure built (a.e.-W now a finite assembly)

**Date**: 2026-06-14 05:55 UTC · **Branch**: `st06` · **HEAD**: `65ee8d5`

## 🎯 What we're doing
Formalizing Stoll [St06] in Lean 4, "for-fun" extension of the complete, axiom-clean Erdős #482 work.
All main theorems done. The live thread is the **cubic self-referential frontier**: does the `√2`/base-2
digit-extraction miracle have a `2^{1/3}` analogue? (Answer trending NO.) The repo is **sorry-free and
custom-axiom-free** — this work *adds* new axiom-clean theorems toward an unconditional cubic
impossibility. This lap built out the **a.e.-`W` route (attack-path #2)** end-to-end infrastructure.

## 🧠 Context to carry forward (the mental model — read this, it supersedes the 0500 handoff's pessimism)
- **The cubic obstruction, precisely.** Digit-reading orbit ⟹ every combined floor-defect
  `D_n = α²f₁+αf₂+f₃` sits in the 2-point set `{C, C−1}` (two parallel hyperplanes in `[0,1)³`) —
  `cubic_orbit_defect_confined`. The `fᵢ` are functions of `2ⁿ(W, αW, α²W) mod 1` (block orbit doubles
  base-2: `uₙ=⌊W2ⁿ⌋`). To contradict, need that triple **equidistributed in `T³`** for a.e. `W`.
- **The reduction I worked out (the key insight of the lap).** Weyl: `T³`-equidistribution ⟺ for every
  `(a,b,c)≠0`, `(1/N)Σ e(2ⁿ·ξW)→0` with `ξ=a+bα+cα²`. **`cubic_lin_indep_int` gives `ξ≠0`** (degree-3
  fact), so this is the **1-D** statement "`{2ⁿs}` equidistributes for a.e. `s`". So:
  **a.e.-`W` cubic ⟸ a.e. equidistribution of `{2ⁿs}`** (+ lin-indep ✓ + a skew-product lift).
- **Two corrections to the 0500 handoff.** (1) It said path #2 "needs a Birkhoff port (large)." WRONG at
  the density level: **a.e. DENSE doubling orbit needs only `AddCircle.ergodic_nsmul`** (no Birkhoff) —
  proved. (2) But density is **NOT enough** for the cubic: the orbit's initial points `(W,αW,α²W)` lie
  on a measure-zero curve, and single-coordinate density doesn't break the 2-plane confinement. The real
  closer is **equidistribution**, reachable via **Davenport–Erdős–LeVeque** (Weyl mean-square +
  Borel–Cantelli, both in mathlib) — NOT general Birkhoff (absent), NOT the open specific-`W` normality.
- **What's genuinely still missing in mathlib:** the pointwise Birkhoff theorem AND the Weyl
  equidistribution *criterion* (`equidist ⟺ all Weyl sums→0`). The latter must be built (Stone–Weierstrass).
- **Abandoned/avoided:** re-running cubic numerics (two prior laps settled `sup N` likely `∞` ⇒
  equidistribution genuinely required); the single-floor "Tier-1" (refuted last lap, `c=½` works).

## ✅ State (all axiom-clean = trust base only; full build 🟢 **8277 jobs**, observed this lap)
11 commits this lap (`ecba3ab`..`65ee8d5`). New axiom-clean decls:
- `General/CubicDefect.lean`: `cubic_orbit_defect_confined`, `cubic_orbit_no_wide_defect_pair`
  (orbit-level ceiling); `cbrt_ne_ratio`, `cube_two_int`, `cubic_lin_indep_int` (degree-3 brick #2).
- `General/DoublingOrbit.lean` (NEW): `ae_orbit_mem_of_isOpen`, `ae_dense_orbit_doubling` (ergodic core,
  no Birkhoff); `doubling_iterate_eq`, `ae_fract_dense_real` (ℝ-line form, ported from Aristotle).
- `General/WeylDoubling.lean` (NEW): `char_int` (orthogonality), `two_pow_inj`, `weyl_double_sum_integral`
  (abstract, dodges a whnf-loop), `doubling_weyl_L2_mean` (`∫₀¹|∑e(k2ⁿs)|²=N`), `term_id`,
  `doubling_weyl_L2_mean_norm`, `doubling_weyl_L2_normalized` (`∫₀¹‖(1/N)∑‖²=1/N` — literally `∫‖g_N‖²`).
- Both new files wired into `Erdos482General.lean`.
- **Aristotle:** `aeL2bc` (`bd44d316`) **RUNNING** = the DEL engine `∑_j∫‖g_j‖²<∞ ⇒ g_j→0 a.e.`
  (Markov+Borel–Cantelli). Provenance: `fractdense`(`17030c67`)→ported as `ae_fract_dense_real`;
  `cubiclinindep`(`e608a9e2`) independently re-proved `cubic_lin_indep_int`; `blockorbit`(`8a6effc3`)
  cross-confirmed the base-2 bounds.

## 🎬 Next actions
1. `aristotle list` (one-shot) → harvest **`aeL2bc` (`bd44d316`)** the moment it's IDLE. Verify in-kernel
   + `#print axioms`, port onto a clean statement, **then submit the next bounded lemma** (keep one in
   flight). Good next Aristotle target: the **Weyl equidistribution criterion** for `AddCircle 1`
   (`equidistributed ⟺ ∀k≠0, (1/N)Σ fourier→0`) — it's the only mathlib-absent piece left in step (b).
2. Assemble **step (b)**: apply the engine to `g_j=(1/j²)∑_{n<j²}e(k2ⁿ·)` (its `∫‖g_j‖²=1/j²` is
   `doubling_weyl_L2_normalized` at `N=j²`; `Summable (1/j²)` is in mathlib) ⇒ `(1/N_j)Σ→0` a.e. along
   `N_j=j²`; fill gaps via `|S_{N+1}−S_N|=1` ⇒ (with the Weyl criterion) a.e. `{2ⁿs}` equidistributes.
3. **Step (c)** (the skew-product lift): use `cubic_lin_indep_int` (`ξ≠0`) to get `T³` joint
   equidistribution of `2ⁿ(W,αW,α²W)` for a.e. `W` ⇒ `(f₁,f₂,f₃)` not confined to the 2 planes
   (`cubic_orbit_defect_confined`) ⇒ **unconditional a.e.-`W` cubic impossibility**. Specific-`W` stays
   open math (don't chase it).

## ⚠️ Gotchas
- DO NOT push (Trevor merges). Commit every green build; pre-commit runs full `lake build` (slow) on
  `.lean` changes, skips doc-only. Verify green from real output before committing.
- **whnf/isDefEq timeout** when commuting a finite sum through an integral if the summand has
  inline-arithmetic casts — abstract the summand (see `weyl_double_sum_integral`; KB note flushed).
- `#print axioms` clean ≠ statement-faithful. The cubic numerics are float-artifact-prone past block ~45
  (use `first_fail_exact`); don't re-run, they're settled.
- Density ≠ equidistribution for the cubic (measure-zero curve) — don't try to close it with the density
  lemmas `ae_dense_orbit_doubling`/`ae_fract_dense_real` alone; they're warm-up/reusable, not the closer.

## 📁 Key files
- `src/Erdos482/General/WeylDoubling.lean` — DEL Weyl bricks (NEW; the lap's main payoff).
- `src/Erdos482/General/DoublingOrbit.lean` — ergodic a.e.-density bricks (NEW).
- `src/Erdos482/General/CubicDefect.lean` — orbit ceiling + lin-indep.
- `PENDING_WORK.md` (★★ SHARPENED ROADMAP at top — the authoritative (a)→(b)→(c) plan) · `STATUS.md`.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor, don't wait for
instructions, and don't offer other KB projects — this doc IS the chosen thread. Absorb the context
above (esp. the density-vs-equidistribution distinction and the (a)→(b)→(c) reduction), then pick up at
Next action #1: `aristotle list` → harvest `aeL2bc` (`bd44d316`), verify it, and drive step (b).**
