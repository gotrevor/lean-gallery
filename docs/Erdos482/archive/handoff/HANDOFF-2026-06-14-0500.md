# Handoff: cubic Tier-1 refuted + Tier-2 scaffold + base-2-normality reframing

**Date**: 2026-06-14 05:00 UTC · **Branch**: `st06` · **HEAD**: `553b312`

## 🎯 What we're doing
Formalizing Stoll [St06] in Lean 4 — the "for fun" extension of the (complete, axiom-clean on `main`)
Erdős #482 / Graham–Pollak work. All St06 main theorems are done. The live thread is the **cubic
self-referential frontier**: does the `√2`/base-2 digit-extraction miracle have a `2^{1/3}` analogue?
(Answer trending: NO, but the unconditional proof is at an open-math wall — see below.)

## 🧠 Context to carry forward (the mental model)
- **The cubic 3-step map** is `v₃=⌊α(⌊α(⌊α(u+c₀)⌋+c₁)⌋+c₂)⌋`, `α=2^{1/3}`, 3-periodic offset schedule
  `(c₀,c₁,c₂)`. For it to read base-2 digits, `v₃−2u ∈ {0,1}` along the orbit.
- **This lap's key catch:** the prior findings doc's proposed "Tier-1" (no constant `c` works for the
  *single-floor* cubic crux) is **FALSE** — `c=½` works for every multiplier `β<2`. Don't re-attempt it.
  The obstruction is purely **multi-floor**.
- **The defect identity localizes everything:** `v₃ = 2u + C − (α²f₁+αf₂+f₃)`, `C:=2c₀+α²c₁+αc₂`,
  `fᵢ`=internal floor errors. So digit `= C − defect`. Valid digit ⟹ defect stays in a width-1 window.
  The combined defect spans width `α²+α+1 > 1` (`cubic_combined_defect_range_wide`), so the wall is
  whether the *orbit* realises that spread.
- **CRITICAL reframing (corrects my own earlier `{α^n ξ}` framing):** the block orbit is **base 2** —
  `u_{n+1}=2u_n+dₙ` ⟹ `uₙ=⌊W·2ⁿ⌋` (proved: `cubic_block_orbit_base_two_bounds`; HOSTCHECK `uₙ/2ⁿ→W≈1.24987`).
  So `f₁≈{αW·2ⁿ}` is a **doubling-map** orbit ⟹ the residual obstruction is **base-2 normality of αW**
  (and α²W), NOT geometric `{α^n ξ}`.
- **Why unconditional is hard (verified, with citation):** for the *specific* `W` (defined by the orbit
  itself), base-2 normality of `αW` is **open math** (like `{(3/2)ⁿ}`). For *a.e.* `W` it's Borel's
  theorem — but mathlib has `×2` ergodicity (`AddCircle.ergodic_nsmul`) yet **NOT** Birkhoff's pointwise
  ergodic theorem (`Dynamics/BirkhoffSum/*` is sum-definitions only). So path #2 needs a Birkhoff port.
- **Numerics caveat:** float64 survival "caps at ~52" is a **2⁵³ precision artifact**; exact
  `Decimal`+`Fraction` gives the doc triple failing at 63 and a tuned schedule surviving ≥91. `sup N`
  (max blocks any schedule survives) is **undecided** — random hill-climbing only lower-bounds it; needs
  constraint-propagation. Use `first_fail_exact`, never float, past block ~45.

## ✅ State (all observed this lap; build 🟢 8275 jobs, every new decl `#print axioms` = trust base only)
- `SelfRefWall.lean`: `onefloor_div2_crux_solvable`/`_solvable_iff`/`_offset_unique`/`_crux_cbrt2`
  (Tier-1 refutation, sharp `β<2` characterization).
- `CubicDefect.lean` (NEW, wired into `Erdos482General.lean`, 14 decls): `cubic_threestep_defect`,
  `cubic_combined_defect_range_wide(_cbrt2)`, `cubicV3`/`cubicDefect`/`cubicV3_sub_eq`,
  `cubic_threestep_digit_pair_fails` (conditional impossibility — the formalizable ceiling),
  `cubic_valid_digits_defects_close`, `cubic_block_orbit_base_two_bounds`, `irrational_cbrt_two`.
- Harvested both ON-LINE-FINDINGS (cubic-literature + equidistribution) → `archive/findings/`; both
  confirm the catches. No `ON-LINE-REQUEST.md` open.
- Aristotle: `onefloor_unique` ✅, `cubicdefect` ✅, `cubicpairfail` ✅ (all cross-confirmed local proofs);
  `blockorbit` (`8a6effc3`) ⏳ RUNNING.
- Last commit `553b312` (doc-only). Last green build observed: 8275 jobs, this lap.

## 🎬 Next actions
1. `aristotle list` (one-shot, NOT the live TUI) → harvest `blockorbit` (`8a6effc3`); verify in-kernel,
   then submit the next bounded lemma to keep one job in flight.
2. Do the **1-D sufficiency probe** (pinned in `PENDING_WORK.md` ▶): does base-2 normality of `αW` ALONE
   force a contradiction at some `c`, or is the 2-D joint statement `({αW2ⁿ},{α²W2ⁿ})` genuinely needed?
   (Expect: 1-D insufficient — `f₂,f₃` compensate. Confirm numerically first.)
3. If pursuing path #2: scope porting Birkhoff's pointwise ergodic theorem (large). Else keep the
   conditional theorem as the ceiling and label the cubic "open in mathematics for specific W."

## ⚠️ Gotchas
- DO NOT push (Trevor merges). Commit every green build. Pre-commit runs full `lake build` (slow) and
  fires on `.lean` under `tools/aristotle/` (not build targets). Doc-only commits skip the build gate.
- `#print axioms` clean ≠ statement-faithful — this lap refuted a plausible cited "Tier-1" claim.
  Numerically verify cubic claims before trusting (and beware float `2⁵³` artifacts — use `first_fail_exact`).

## 📁 Key files
- `src/Erdos482/General/CubicDefect.lean` — the Tier-2 scaffold (NEW).
- `src/Erdos482/General/SelfRefWall.lean` — single-floor solvability suite + Tier-1 refutation.
- `PENDING_WORK.md` (★ ACTIVE FRONTIER + ▶ next-lap first move) · `notes/CUBIC-EXPLORATION.md` ·
  `tools/sandbox/cubic_uniform_escape_probe.py` (`first_fail_exact`, float-artifact caveat) ·
  `STATUS.md` (axiom ledger) · `archive/findings/…equidistribution-xi-theta-n.md` (Koksma confirmation).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or offer other
KB projects — this doc IS the chosen thread. Absorb the context, then pick up at "Next actions" #1
(`aristotle list` → harvest `blockorbit`), keep one Aristotle job in flight, and drive the cubic frontier.**
