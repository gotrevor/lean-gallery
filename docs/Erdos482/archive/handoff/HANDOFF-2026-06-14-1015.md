# Handoff: cubic AND quartic self-referential impossibilities — BOTH COMPLETE & axiom-clean

**Date**: 2026-06-14 10:15 UTC · **Branch**: `st06` · **HEAD**: `e9b4ab8` (+ this doc)

## 🎯 What we're doing
Formalizing Stoll [St06] in Lean 4 — the "for-fun" frontier is the unconditional a.e.-`W`
self-referential impossibility (attack-path #2). All #482/St05/St06 headlines were already done.
**This lap finished the entire CUBIC thread AND then generalized the whole thing to the QUARTIC
(degree-4) case.** Both are end-to-end machine-checked with **zero custom axioms** — every headline
depends only on `[propext, Classical.choice, Quot.sound]`. The repo is now **fully sorry-free and
axiom-free** (`grep -rnw sorry src/` and `grep -rnE '^\s*axiom' src/` both empty).

## 🧠 Context to carry forward (the mental model)
- **The whole obstruction, in one breath.** For `α=2^{1/d}` (`α^d=2`), the `d`-step floor map reads `W`'s
  base-2 digits iff the digit `v_d−2u ∈ {0,1}` at every block `u=⌊W·2ⁿ⌋`. The defect collapses: the LAST
  floor error is forced (`{C−g}`), so the digit `= ⌊C−g⌋`, and digit∈{0,1} ⟺ the **partial defect**
  `g = α^{d-1}f₁+…+αf_{d-1}` lies in the width-2 window `(C−2,C]`. But `g` ranges over `[0, α^{d-1}+…+α)`,
  width `>2`, so a DENSE orbit leaves the window → contradiction. The density (`ae_W_…_torus_orbit_dense`)
  is **schedule-independent** — that's why one a.e. set defeats ALL schedules at once (quantifier moves
  inside `filter_upwards`).
- **Why the quartic went fast.** The analytic engine (`MultidimWeyl`, `EquidistDense`, `DELEngine`,
  `DoublingEquidist`) is already degree-agnostic (`Tᵈ`). So generalizing = re-deriving the per-degree
  *algebra*: the `d`-floor defect identity (`linear_combination h_d + α h_{d-1} + … + (u+c0)·hα`), the
  g-collapse, the orbit-coordinate identities, the geometry crux (`fract_shift_realize` reused verbatim),
  and the `Fin d` torus assembly. All mechanical mirrors of the cubic files.
- **The one genuinely-hard quartic brick: `quartic_lin_indep_int`.** `{1,2^{1/4},2^{2/4},2^{3/4}}` ℤ-indep.
  Aristotle stalled (2% after 13 min) — it's real number theory. I proved it locally via **infinite 2-adic
  descent** (group→square→norm-system `4be=a²+2c², b²+2e²=2ac`→all-even-and-halve via
  `Nat.strong_induction_on`). No Eisenstein needed (mathlib's Kummer `X_pow_sub_C_irreducible` is TODO at
  `p=2`). See memory `[[lean-zlinindep-rpow-descent]]`. **This descent is the per-degree obstacle that does
  NOT obviously generalize** — for general `d` you'd want `Irreducible (Xᵈ−2)` (Eisenstein) instead.
- **Aristotle**: I used it for the cubic geometry crux (`7b1ff2ad`, harvested+ported), and submitted
  `binary_floor_eq` (`b8320752`) and `quartic_lin_indep_int` (`2dcaaab1`) but **beat both locally and
  cancelled them**. No jobs running now. Lesson: for self-contained algebra/number-theory I'm often faster
  than waiting; reserve Aristotle for genuinely hard analytic lemmas.

## ✅ State (full `lake build` 🟢 8290 observed, all axiom-clean)
- **Cubic** (`CubicFinish.lean` + `BinaryExpansion.lean`): `ae_no_cubic_schedule_reads_base_two` (uniform
  over schedules), `ae_not_cubicDigitRepresentable`, `ae_not_cubicReadsBaseTwo`,
  `ae_not_cubicRecurrenceRepresentable` (genuine recurrence via `binary_floor_eq`).
- **Quartic** (`QuarticDefect/DefectLink/TorusEquidist/Finish.lean`): `ae_no_quartic_schedule_reads_base_two`
  + `ae_not_quartic{DigitRepresentable,ReadsBaseTwo}`. Plus `quartic_lin_indep_int` (descent),
  full T⁴ equidistribution, geometry crux, continuity glue.
- 15 commits this lap, every one green (pre-commit runs full `lake build`). HEAD `e9b4ab8`.

## 🎬 Next actions
1. **Decide the next frontier** (PENDING_WORK.md ▶▶, top section). The cleanest remaining target:
   **general degree-`d`** — a single `Fin d`-parametrized formalization replacing the hand-rolled
   per-degree files. Needs general `{1,…,α^{d-1}}` ℤ-indep (`Irreducible (Xᵈ−2)` via Eisenstein — the
   descent won't generalize cleanly), a `d`-fold `linear_combination` defect identity, and a `Fin d`
   assembly. This is the elegant capstone but a real multi-lap build.
2. **OR** hand-roll `d=5,6` if a uniform version is too ambitious — fast now that the pattern is proven.
3. **Fixed-`W` version stays OPEN MATH** (Borel normality, not in mathlib) — don't attempt; literature lap.

## ⚠️ Gotchas (all flushed to memory staging)
- Copy-porting a file under the same namespace: rename degree-specific helpers or hit
  "environment already contains 'Foo.x'" on triple-import (`[[lean-namespace-collision-multi-import]]`).
- `Fin.val_three` doesn't exist → `have v3 : ((3:Fin 4):ℕ)=3 := rfl`. `![…]` breaks across line-wraps in
  `set` (keep one line). rpow abbrev needs `noncomputable section`. A file using `volume`/`∀ᵐ` needs
  `open MeasureTheory` (else "Function expected at Measure").
- `simp only [def, def2]; ring` errors "no goals" if the simp already closed it (g-collapse `hsplit`).
- DO NOT push (Trevor merges). Pre-commit runs full `lake build`; commit every green build.

## 📁 Key files
- `src/Erdos482/General/CubicFinish.lean` — cubic headline + 4 forms + recurrence capstone.
- `src/Erdos482/General/BinaryExpansion.lean` — `binary_floor_eq` (reusable binary-orbit↔floor identity).
- `src/Erdos482/General/QuarticDefect.lean` — quartic defect/window + `quartic_lin_indep_int` (descent).
- `src/Erdos482/General/QuarticFinish.lean` — quartic headline + capstones + geometry crux + glue.
- `PENDING_WORK.md` (top ★★★★) — authoritative state + the general-`d` frontier plan.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor, don't wait for
instructions, and don't offer other KB projects — this doc IS the chosen thread. The cubic AND quartic
are DONE and axiom-clean; the repo has no open sorries/axioms. Pick up at Next action #1: decide between
the uniform general-`d` build (elegant, multi-lap, needs Eisenstein for `Irreducible (Xᵈ−2)`) vs.
hand-rolling `d=5`, then start building.**
