# Handoff: Erdős #482 — Stoll Thm 3.2 + Cor 3.3 COMPLETE & axiom-clean (the bonus is done)

**Date**: 2026-06-06 19:46 UTC · **Branch**: main

## 🎯 What we're doing
Formalize Stoll's *A fancy way to obtain the binary digits of 759250125√2* (arXiv:0902.4168),
which generalizes Graham–Pollak (Erdős #482). The **headline** (digits of √2) was done in a prior lap.
This lap took the **BONUS** — which the prior handoff called "genuinely paper-blocked" — from nothing
to a **complete, axiom-clean formalization**: Theorem 3.2 (all pairs) and **Corollary 3.3 made
UNCONDITIONAL** (the digits of 759250125√2, the paper's title result).

## 🧠 Context to carry forward
- **The bonus is essentially finished.** Don't go looking for the paper online (the findings doc was
  harvested → `archive/findings/`); don't re-request it. Everything is in `src/Erdos482/Stoll.lean`.
- **The key reframe that unlocked everything:** I rederived a cleaner invariant than the paper's
  `(α,β,l,γ,t)` table. Stated purely in `α√2`, the digit-extraction induction (`stoll_pair`,
  `stoll_digit`) needs ONLY `α:ℤ`, the universal interval `ε∈[1−√2/2,√2/2)`, and a per-pair base case
  — **not** `β`, `γ`, or `α+β=2^{l+1}` (those only relabel α√2's digits as `t_i`'s, handled separately
  by `binDigit_div_pow`). ½-step = `crux` (eq 7); ε-step = `eq8_general` (eq 8), both already existed.
- **How Cor 3.3 became unconditional** (this was the wall): the 62-step base case for pair 6 holds in a
  razor-thin ε-interval `[0.50124,0.51035)`. But `ε=1−π²/e³≈0.50862` is INTERIOR, so margins are
  comfortable (worst ≈0.0024). Pin √2 to 13 digits + ε via mathlib's π/e bounds → every step is
  `linarith` (the only var×var product `√2·ε` is bounded once). See the flushed insight
  `lean-interior-eps-base-case-trick`. The base lemma `cor33_base_of_bounds` is **script-generated**.
- **Numerics first, always.** I verified every identity/formula in Python (`python3` is available)
  before writing Lean — caught that the paper's eqs (5)/(6) were faithful but my clean invariant was
  cleaner; confirmed all base values; found the interior-ε margins. Do the same for anything new.
- Pairs 3,4,7 base cases use `nlinarith` against the EXACT √2-expressed ξ endpoints (the half-open
  `ε<ξ₂` is an exact algebraic identity via √2·√2=2), with `set_option maxHeartbeats` bumped.

## ✅ State
- `lake build` **GREEN** (verified via pre-commit gate on every commit this lap + a final full build).
- **Zero `sorry`, zero custom `axiom`** across the whole repo (grep-verified). Every top-level result
  `#print axioms` = `[propext, Classical.choice, Quot.sound]` — checked `graham_pollak`,
  `stoll_digit`, `cor33_unconditional` this session.
- New this lap in `src/Erdos482/Stoll.lean`: `vv` (parametrized seq), `stoll_pair`/`stoll_digit`
  (general core), `stoll_pair1/2/3/4/7/8` (all 7 non-special pairs), `cor33` (conditional),
  **`cor33_unconditional`** (title result, no hypotheses), `binDigit_div_pow` (t_i framing),
  `floor_mul_sqrt2`, `cor33_base_of_bounds`, `sqrt2_lo/hi`, `cor33_eps_lo/hi`, `vv_even_to/vv_odd_to`.
- HEAD `1e4e434`. 8 commits this lap (see `git log`).
- **Aristotle:** job `31ac9af6` (tconn = `binDigit_div_pow`) IN_PROGRESS but **now redundant** (I
  proved it locally). `cor33mem` (7fa61a30) returned COMPLETE_WITH_ERRORS — ignore, solved locally.

## 🎬 Next actions
The bonus is complete; remaining work is optional polish / a research-hard refactor. In priority order:
1. **Check Aristotle `31ac9af6`** (`aristotle tasks 31ac9af6-…`). If it returns a clean proof of
   `binDigit_div_pow`, it's just a cross-check — diff against ours, don't bother porting. Then, since
   no job will be running, architect the next bounded lemma OR accept there's no clean leaf left.
2. **Optional (cosmetic):** state Cor 3.3 in the literal `t_6` form via `binDigit_div_pow` (a=759250125,
   b=314491699, l=29). I skipped it to avoid `(759250125:ℝ)` vs `((759250125:ℤ):ℝ)` cast-wrangling —
   `push_cast`/`norm_cast` first if you do it.
3. **Research-hard (deferred, in `PENDING_WORK.md` §2):** generalize the base case abstractly (Stoll's
   eq 9) so pair 6 holds "for all ε in [ξ₁,ξ₂)" and all pairs follow uniformly. This is the only
   genuinely open mathematical thread; it needs the eq-9 endpoint analysis.

## ⚠️ Gotchas
- `cor33_base_of_bounds` and `stoll_pair7_base` carry `set_option maxHeartbeats 1000000`/`4000000` —
  the long step-chains exceed the 200k default (reported as `timeout at whnf` at the *declaration*
  line, which is cumulative, not one bad step). Keep these when editing.
- Base-case lemmas are **script-generated** — regenerate with a Python loop, don't hand-edit 62 steps.
  Watch Python `Fraction` auto-reduction of literals (`508622/1000000`→`254311/500000`): the bound
  lemmas must use the SAME reduced literal as the base lemma's hypothesis or unification fails.
- mathlib v4.29.1 names: `pow_le_pow_left₀` (not `pow_le_pow_left`); `Real.pi_gt_d6`/`pi_lt_d6` (NOT
  `pi_gt_3141592`); `Real.exp_one_gt_d9`/`lt_d9`; `Int.floor_eq_iff` takes NO hypothesis here.

## 📁 Key files
- `src/Erdos482/Stoll.lean` — the entire bonus (≈970 lines). Read its header docstring + `STATUS.md`.
- `src/Erdos482/{Crux,Induction,Digits,Main,Basic}.lean` — headline + reused infra (`crux`,
  `eq8_general`, `binDigit`, `vv`'s sibling `u`).
- `STATUS.md` — full inventory (refreshed). `PENDING_WORK.md` — the 3 remaining (optional) threads.
- `archive/findings/ON-LINE-FINDINGS-2026-06-06-stoll-thm32-cor33.md` — the consumed paper transcription.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. The bonus is
DONE; start at Next actions #1 (poll Aristotle `31ac9af6`), then if nothing's actionable, pick up the
research-hard thread in `PENDING_WORK.md` §2 (abstract eq-9 base case) and make real progress on it —
do NOT declare the work exhausted.**
