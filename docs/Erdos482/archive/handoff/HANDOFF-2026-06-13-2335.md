# Handoff: Thm 3.4 faithfulness CORRECTED + self-referential wall formalized

**Date**: 2026-06-13 23:35 UTC · **Branch**: `st06` · **HEAD**: `420ae33`

## 🎯 What we're doing
Formalizing Stoll [St06] (*Acta Arith.* 125 (2006), 89–100) in Lean 4 — the "for fun" extension of
the (complete, axiom-clean on `main`) Erdős #482 / Graham–Pollak work. This lap was a **correction
lap**: the prior baton declared "St06 COMPLETE, no open obligation," but that was wrong.

## 🧠 Context to carry forward
- **The load-bearing lesson: `#print axioms` clean ≠ statement-faithful.** An unharvested ON-LINE
  findings doc proved the repo's **Theorem 3.4** had formalized a *swapped recurrence* — `ε` on the
  b-step, which is Theorem **3.3**'s placement. Stoll's actual 3.4 puts `ε` on the **a-step**. The
  prior lap's celebrated "Diophantine obstruction / only ε=½ works for all w" was a faithful proof of
  an *unfaithful* statement. Whenever this repo (or any) claims an "obstruction / not-t-universal"
  result, check the recurrence matches the paper BEFORE trusting it.
- **The `su` def already had ε on the a-step.** `St06Example.su a b ε s m` applies `(a,ε)` at `Even n`
  and `(b,s)` at odd `n`. So `su a b ε (1/2) m` IS Stoll's genuine 3.4 — no infrastructure change
  needed; I just had to prove the a-step crux at general ε with the interval condition.
- **Why the genuine 3.4 interval is t-universal (not Diophantine):** ε on the a-step gives the floor
  bracket `0 ≤ Dε − (t+2m)k + l(2B−ts) < t+2m`, `D=(t+2m)(2k+1)+2l`, `2B−ts∈(−2,0]`. Bounding `t` and
  `2B−ts` *independently* over their full ranges (both worst cases bind at `t=1`) yields Stoll's
  printed symmetric interval `½ ± (m−l+½)/D₁`, `D₁=(2m+1)(2k+1)+2l`. No equidistribution input. (The
  swap had moved ε onto the b-step, whose bracket is genuinely non-uniform — manufacturing a fake
  pair-5-style obstruction.) HOSTCHECK numerics confirm: paper recurrence's digit-correct ε range
  *contains* Stoll's interval; swapped recurrence collapses to ≈[0.49,0.50].
- **Faithfulness sweep otherwise clean:** Thm 3.3 (ε on b-step) IS faithful — that's genuinely 3.3's
  shape; pair 5 in `Stoll.lean` is genuinely Diophantine (different paper, arXiv:0902.4168). Only 3.4
  had the swap.
- **The cubic frontier, engaged structurally (new math).** Instead of re-chasing the numeric cubic
  wall, I proved *why* self-reference is special: see SelfRefWall below. The cubic `2^{1/3}` 3-step map
  remains genuinely open (3 nested floors can't align; `cubic_recover.py` shows the tracked `W` isn't
  algebraic-clean) — filed for literature in `ON-LINE-REQUEST.md`.

## ✅ State (all observed this lap; build 🟢 8274 jobs, all new theorems `#print axioms` = trust base)
- **Genuine Theorem 3.4** (`src/Erdos482/General/St06Thm34.lean`): `st06_thm34_astep_eps` (a-step crux,
  every ε in the symmetric interval, all t∈[1,2)), `st06_thm34_{closed,digits,isBit,even_digits}_eps`
  (full t-universal theorem), `st06_thm34_sqrt2_eps_nonhalf` (concrete ε=9/20≠½ → √2's binary digits).
  Old ε-on-b-step theorems re-labeled `[SWAPPED-VARIANT, NOT Thm 3.4]` (kept as documented contrast).
- **NEW `src/Erdos482/General/SelfRefWall.lean`** (wired into `Erdos482General.lean`):
  `selfref_crux_fails_of_three_le` (g-analogue crux fails ∀c for g≥3), `selfref_crux_solvable_iff`
  (solvable iff g=2), `selfref_crux_offset_unique` (then c=½ forced). ⇒ `⌊√2(u+½)⌋` is THE unique
  self-referential digit recurrence.
- Findings doc harvested → `archive/findings/`. STATUS.md, module headers, `notes/CUBIC-EXPLORATION.md`
  all corrected. `ON-LINE-REQUEST.md` created (cubic literature). ~13 commits, last green at `420ae33`.
- **Aristotle**: `thm34astep` (`d9a743f9`) ✅ and `selfrefwall` (`8c772507`) ✅ both independently
  re-proved this lap's two central theorems, axiom-clean. `selfrefunique` (`121e5a16`) RUNNING.

## 🎬 Next actions
1. `aristotle show 121e5a16` — confirmatory cross-check of `selfref_crux_offset_unique` (already proven
   locally + axiom-clean). When done, submit the next bounded lemma to keep one job in flight.
2. Check for an `ON-LINE-FINDINGS-*-cubic*.md` answering the cubic literature request; if present,
   harvest it and decide: formalize a cubic *impossibility* (hard — 3-step map, nested `2^{1/3}` floors,
   far beyond SelfRefWall's two-witness argument) vs. pursue a positive Pisot/β-expansion construction.
3. If no findings and no other thread: optional polish (top-level St06 showcase re-exporting genuine
   Thm 3.4 + SelfRefWall + `erdos482_resolution`).

## ⚠️ Gotchas
- DO NOT push (Trevor merges); commit every green build. Pre-commit hook runs full `lake build` (slow);
  it also fires on `.lean` files under `tools/aristotle/` even though they aren't build targets.
- Don't re-trust "COMPLETE" claims without a faithfulness check (see Context).

## 📁 Key files
- `src/Erdos482/General/St06Thm34.lean` — genuine Thm 3.4 (`*_eps`) + re-labeled swapped contrast.
- `src/Erdos482/General/SelfRefWall.lean` — self-referential characterization suite (NEW).
- `STATUS.md` (axiom ledger) · `notes/CUBIC-EXPLORATION.md` · `tools/sandbox/st06_thm34_HOSTCHECK.py`
  (ground truth) · `archive/findings/ON-LINE-FINDINGS-2026-06-13-thm34.md` (the catch).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. Absorb the
context above, then pick up at "Next actions" #1 (`aristotle show 121e5a16`) and continue.**
