# Handoff: St06 COMPLETE — every theorem formalized & axiom-clean (Cor 3.5 + Thm 3.4 closed this lap)

**Date**: 2026-06-13 22:55 UTC · **Branch**: `st06` (the "for fun" St06 extension; #482 core is done & axiom-clean on `main`) · **HEAD**: `a54b13b`

## 🎯 What we're doing
Formalizing **Stoll [St06]** (*On a problem of Erdős and Graham concerning digits*, Acta Arith. 125
(2006), 89–100) in Lean 4 — the optional extension of the (complete) #482 work. **As of this lap the
St06 extension is COMPLETE**: Example 1.1, Thm 3.1 (all 12 cones), Thm 3.3 (full), Thm 3.4 (ε=½ +
full-interval obstruction), Cor 3.5 (Beatty capstone) — all formalized, **zero `sorry`, zero custom
axiom**, every headline `#print axioms` = `[propext, Classical.choice, Quot.sound]`. Build 🟢 green
(8273 jobs, observed). **There is no open mathematical obligation left in this repo.**

## 🧠 Context to carry forward (the mental model)
- **Cor 3.5's "PDF-blocked index convention" was illusory** (prior handoff's #2). Don't chase the PDF.
  KEY INSIGHT, found numerically: the GP recurrence `su √2 √2 ½ ½ m` started at the Beatty value
  `m = ⌊r·α⌋` (`α ∈ {1+√2, 1+1/√2}`) reads off the binary digits of **`r·√2`**. The number it tracks is
  `w(m) = r·α` (the Beatty real itself), and Stoll's printed `w`-table is just a *mantissa
  renormalization* of the same digit string. So the digit sequence is `binDigit(r√2)` — clean, exact.
- **The engine = `gp_pair` (the classical r=1 Graham–Pollak) generalized by a free factor `r ∈ ℕ`.**
  Because `Erdos482.crux` and `Erdos482.eq8_general` are stated for a *free* `x`/`w`, inserting `r` is
  mechanical: same floor-step lemmas (`cor35_floorA`/`cor35_floorB`) with the `linear_combination`
  scaled by `r·2^p`; the base step `cor35_base` reduces to `crux (√2·r)`. Case 2 (start `⌊√2 r⌋+r`)
  uses the same lemmas with a half-step phase offset.
- **Thm 3.4's full k-dependent interval is NOT a t-universal theorem** — resolved exactly like pair 5.
  The b-step lands iff `frac ∈ (−d, 1−d]` (`d = C−2B ∈ {0,1}`); the `d=0` (large `t·s−2B`) and `d=1`
  (small) boundary branches pull ε in *opposite* directions, meeting only at ε=½. So ε=½ is the proven
  ceiling; the obstruction (`band_fails_below/above_half`) is machine-checked. Numerics: Stoll's printed
  interval is ~28× wider than the actual √2 band. **Do not formalize against the printed interval.**
- **Abandoned/closed**: the cubic generalization (2^{1/3}, 3-step map) — explored, NEGATIVE result
  (`notes/CUBIC-EXPLORATION.md`): a 3-phase offset triple gives an approximate bitstream that breaks at
  j=64; the √2 two-step crux has no uniform cubic analogue ("needs new math", not a formalization gap).

## ✅ State (all observed this lap)
- Full `lake build` green (8273 jobs); comprehensive `#print axioms` sweep = trust base only everywhere.
- New this lap (`src/Erdos482/General/St06Cor35.lean`): `cor35_pair`/`_case2`, `cor35_floorA/B`,
  `cor35_base`, `cor35_digits_case1/case2`, `beatty_start_case1/case2`, capstone **`st06_cor35`**,
  `st06_cor35_realDigits` (= `Real.digits (r√2) 2 j`), `st06_cor35_isBit`, `st06_cor35_recovers_gp`
  (r=1→√2), `binDigit_three_sqrt2_first_four` (r=3→3√2 = …0,0,1,1₂).
- New (`St06Thm34.lean`): `st06_thm34_bstep_value`, `_bstep_band`, `_band_fails_below/above_half`,
  `st06_thm34_isBit`. New (`St06Thm33.lean`): `st06_thm33_isBit`.
- **Aristotle**: 3 jobs cross-validated my local proofs (`cor35_pair`, `bstep_value`, `bstep_band` — each
  from `crux` alone). One still in flight: `cor35cap` (`5d48c0b1-fb0f-4a50-80b9-820a17ae9b31`),
  cross-validating the `st06_cor35` Beatty case-split.

## 🎬 Next actions (ALL optional polish — no open math; confirm with Trevor whether St06 is "done")
1. Poll `aristotle list` for `5d48c0b1`; if returned, `aristotle show` it (confirmatory only — already
   proven locally). Then keep one Aristotle job in flight if continuing.
2. Optional polish: a single top-level St06 showcase module re-exporting `st06_cor35` +
   `erdos482_resolution`; a unified Thm 3.3/3.4/Cor 3.5 statement (heterogeneous, may be artificial).
3. Non-blocking curiosity (`ON-LINE-REQUEST.md`): does Stoll's *printed* Thm 3.4 interval claim a per-`w`
   Diophantine result or is it a transcription artifact? Either answer leaves our content intact.
4. Genuinely-open research (multi-lap): self-referential digit recurrences for non-quadratic algebraic
   numbers — wall mapped in `notes/CUBIC-EXPLORATION.md`.

## ⚠️ Gotchas
- DO NOT push (Trevor merges); commit every green build. Pre-commit hook runs `lake build` (slow gate).
- `St06Cor35.lean` imports `Erdos482.Induction` + `Erdos482.Digits` (for `crux`/`eq8_general`/`binDigit`/
  `digits_eq_floor_sub`/`binDigit_mem_zero_one`/`realDigits_eq_digitStep`/`digitStep_mem`).
- `<;> norm_num` after `rw` (not `; norm_num`) when `rw` may auto-close some goals via rfl inconsistently.

## 📁 Key files
- `src/Erdos482/General/St06Cor35.lean` — Cor 3.5 capstone + engine. `St06Thm34.lean` — ε=½ + obstruction.
- `STATUS.md` — living overview + axiom ledger (refreshed this lap). `PENDING_WORK.md` §0′ — all ✅.
- `notes/ST06-THM34-FINDINGS.md`, `notes/CUBIC-EXPLORATION.md` — the two resolutions / the mapped wall.
- `tools/sandbox/st06_cor35_*.py`, `st06_thm34_band.py`, `cubic_*.py` — ground-truth numerics.

---
**→ Next session: this is your starting point. St06 is COMPLETE & axiom-clean — there is no open
obligation. Don't summarize this doc back to Trevor, don't wait for instructions, and don't offer other
projects from the KB. Start at Next action #1 (poll/verify the `cor35cap` Aristotle job), then either do
an optional polish item or, with Trevor, decide St06 is done. Do NOT try to prove Stoll's printed Thm 3.4
interval — it is provably not t-universal (the obstruction is already in the repo).**
