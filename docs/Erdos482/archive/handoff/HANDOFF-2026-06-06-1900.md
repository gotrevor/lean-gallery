# Handoff: Erdős #482 — headline COMPLETE & axiom-clean; bonus paper-blocked

**Date**: 2026-06-06 19:00 · **Branch**: main

## 🎯 What we're doing
Formalize Graham–Pollak (Erdős #482): for `u 0 = 1`, `u(n+1) = ⌊√2·(u n + 1/2)⌋`,
`u(2n+1) − 2u(2n−1)` is the n-th binary digit of √2. **This lap took it from an all-`sorry`
skeleton to a complete, axiom-clean formalization (~18 theorems, zero sorries, zero custom axioms).**

## 🧠 Context to carry forward
- **The HANDOFF's original identities (5)/(6) had off-by-one exponents.** The faithful, numerically
  verified forms (now proved as `gp_pair`) are `u(2j+1)=⌊√2·2^j⌋+2^j`, `u(2j+2)=⌊√2·2^j⌋+2^(j+1)`.
  Always sanity-check a digit-index statement numerically before trusting it.
- **Proof architecture that worked:** recurrence over ℤ (`urec`: nonneg arg ⇒ `Nat.floor=Int.floor`);
  each induction step is `⌊↑z + c⌋ = z` with `c∈[0,1)` supplied by `crux` (even→odd, the universal
  eq 7) or `eq8` (odd→even). The fractional-offset algebra closes by
  `linear_combination (2:ℝ)^… * (hs2 : √2*√2=2)` after `rw [← Int.self_sub_floor]; push_cast`.
- **The bonus (Stoll Thm 3.2 / Cor 3.3) is genuinely paper-blocked** — I verified the parametrization
  is NOT guessable: changing the additive constant ε away from 1/2 breaks the identities (ε=1−√2/2
  gives u₁=1≠2). Do not fabricate it. The request is filed in `ON-LINE-REQUEST.md`.
- The general machinery for the bonus is already in place: `crux` is the general eq (7),
  `eq8_general` is the full-ε-interval eq (8). Only the faithful per-pair statement + replayed
  `gp_pair` induction remain once the paper text arrives.

## ✅ State
- `lake build` **GREEN** (verified via pre-commit gate on every Lean commit this lap). Tree clean.
- Zero `sorry`, zero custom `axiom`. Every theorem `#print axioms` = `[propext, Classical.choice, Quot.sound]`.
- HEAD: the last commit is the HANDOFF-doc-pointer docs commit; `graham_pollak` etc. all built.
- **STATUS.md has the full current theorem inventory** — read it first. Highlights: `graham_pollak`,
  `graham_pollak_digits`, `gp_digit_seq`, `gp_reconstructs_sqrt2`, `digits_eq_floor_sub` (general),
  `binDigit_mem_zero_one`, `u_pos`/`u_strictMono`, `irrational_fract_sqrt2`,
  `digits_two_irrational_not_eventually_zero` (+√2 corollary), `digits_two_not_eventually_one`,
  `gp_diff_one_infinitely`, `binDigit_sqrt2_first_six` (concrete `0,1,1,0,1,0`).
- **Aristotle:** `fca57695` (digit_bridge) & `96d83ca4` (digits_eq_floor_sub) DONE + verified
  axiom-clean (cross-checks). `7ef053f3` (irrnonterm = general non-termination) **IN FLIGHT**.

## 🎬 Next actions
1. `aristotle list` → if `7ef053f3` is IDLE, `aristotle download` it, extract, verify in our kernel
   + `#print axioms`, compare to the local `digits_two_irrational_not_eventually_zero` (already proved).
2. Check repo root for `ON-LINE-FINDINGS-*.md` (Stoll Thm 3.2 answer). If present: harvest, then
   formalize Thm 3.2 per `PENDING_WORK.md`'s three paths (replay `gp_pair` per (α,β,l,γ) pair).
   Then `git mv` the findings doc to `archive/findings/`.
3. If still no paper answer: submit the next bounded Aristotle lemma (keep one in flight) and pick
   any forward-useful, faithfully-statable lemma; do NOT guess the Thm 3.2 parametrization.

## ⚠️ Gotchas
- `Int.floor_sub_int` does NOT exist → use `Int.floor_sub_intCast`. `Real.digits` val: unfold with
  `simp only [Real.digits, Fin.val_ofNat, Nat.cast_ofNat]` (the base 2 appears as `↑2`).
- `field_simp` can fully close a goal → a trailing `ring` then errors "no goals"; drop it.
- `Nat.le_floor` is `(h : (n:α) ≤ a) → n ≤ ⌊a⌋₊` (not an iff). `pow_le_pow_right₀`, `exists_pow_lt_of_lt_one`,
  `one_div_le_one_div_of_le` are the Archimedean kit used in the non-termination proofs.

## 📁 Key files
- `src/Erdos482/Crux.lean` — `crux` (the whole proof in miniature).
- `src/Erdos482/Induction.lean` — `gp_pair`, `floorA`/`floorB`, `eq8`/`eq8_general`, `u_pos`, `u_strictMono`.
- `src/Erdos482/Digits.lean` — `digits_eq_floor_sub`, `digit_bridge`, irrationality + non-termination.
- `src/Erdos482/Main.lean` — `graham_pollak` + canonical/certificate corollaries.
- `STATUS.md` (full inventory), `PENDING_WORK.md` (bonus 3 paths), `ON-LINE-REQUEST.md` (paper ask).
- `tools/aristotle/` — Aristotle problem dirs.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. Start at
Next actions #1: `aristotle list` to check job `7ef053f3`, then check for `ON-LINE-FINDINGS-*.md`.**
